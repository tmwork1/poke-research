# 開発ロードマップ — 情報ハブ（Information Hub）構想に基づく版

## 目的
このプロジェクトは、ポケモンプログラミングに関する技術情報を収集・整理・検索する情報ハブを目指します。

## 進捗状況
- 全体: M1 は完了。次は M2 のフィルタ／全文検索、タグ付け、簡易 UI に着手する段階。
- 進捗の見方: `完了` / `進行中` / `未着手` で管理する。

| マイルストーン | 状況 | 補足 |
| --- | --- | --- |
| M1: DB スキーマとマイグレーション、基本 CRUD API | 完了 | ローカル Supabase で CRUD スモークテストまで確認済み |
| M2: フィルタ／全文検索、タグ付け、簡易 UI、主要画面 | 未着手 | 画面作成は計画に含め済み |
| M3: CSV/API インポーター、収集ジョブ、provenance 付与 | 未着手 | データ収集の自動化フェーズ |
| M4: アクセス制御、エクスポート、編集履歴、監査ログ | 未着手 | 運用面の強化フェーズ |
| M5: CI/CD、バックアップ、ドキュメント整備 | 未着手 | リリース/運用安定化フェーズ |

## 要件サマリ
- コンテンツ中心: `sources` / `items` を中心に、注釈を付与する。
- メタデータ重視: 出典、収集日時、バージョン、ライセンスを扱う。
- 検索性: メタデータ検索、全文検索、タグ、フィルタで探せること。
- 再現性と履歴: 更新履歴と provenance を残す。
- 共有とアクセス制御: 公開／非公開、ユーザー／チーム単位の管理を見据える。

## コア概念
- Source: データの出典。
- Item: Source から取り込んだ個別レコード。
- Annotation: Item に付与されるタグやコメント、評価。
- Provenance: 収集時の情報。

## データモデルの方針
- `items` は汎用テーブルとして扱う。
- 種別の差分は `kind` と `metadata` で表現する。
- 関連性は `item_relations` で表す。
- AI 要約や分類結果は `annotations` に寄せる。

## マイルストーン
- M1: DB スキーマとマイグレーション、基本 CRUD API（sources/items/annotations）。
- M2: フィルタ／全文検索、タグ付け、簡易 UI、主要な画面（トップ、一覧、詳細、検索結果）の作成。
- M3: CSV と API インポーター、収集ジョブ、provenance 付与。
- M4: アクセス制御、エクスポート、編集履歴と監査ログ。
- M5: CI/CD でのマイグレーション自動化、バックアップ運用、ドキュメント整備。

## 詳細な開発計画
### M1: 基盤整備
- スキーマを確定し、`sources` / `items` / `annotations` を中心に初期マイグレーションを整える。
- API の入出力形式を揃え、CRUD の最低限の振る舞いを通す。
- ローカル開発用の接続確認、マイグレーション適用、CRUD スモークテストまでを通しで確認する。

### M2: 検索と画面の初期実装
- 一覧画面での絞り込み条件を固め、検索条件を API に渡せるようにする。
- 全文検索とタグ検索を追加し、結果一覧と詳細遷移をつなぐ。
- トップ、一覧、詳細、検索結果の 4 画面を作り、主要導線をひと通り確認できる状態にする。

### M3: 収集・取り込みの自動化
- CSV と外部 API からの取り込み経路を分けて実装する。
- 収集ジョブを定期実行できる形にし、失敗時の再実行手順も用意する。
- provenance を保存し、どのソースからいつ取り込まれたか追跡できるようにする。

### M4: 運用機能の追加
- 公開／非公開や権限の境界を整理し、アクセス制御を導入する。
- 編集履歴と監査ログを追加し、変更の追跡性を高める。
- 必要なデータを CSV などで出力できるようにする。

### M5: リリース準備と安定化
- CI/CD でマイグレーションと基本検証を自動化する。
- バックアップと復旧の運用手順を確立する。
- README、運用メモ、開発手順を整備して、引き継ぎしやすい状態にする。

### 横断的に継続すること
- スキーマ変更時は、API と画面の整合性も同時に確認する。
- 収集データは provenance とライセンス条件を必ず確認する。
- 各マイルストーンごとに `docs/progress/YYYY-MM-DD.md` を更新し、完了条件を明確にする。

## 運用方針
- タスク管理: `manage_todo_list` を使う。
- 進捗記録: `docs/progress/YYYY-MM-DD.md` に残す。
- ガバナンス: 外部データの利用条件を守り、個人情報や機密情報は取り込まない。

## 次のアクション（短期・具体）
1. M2 の最初の実装対象を決める。
2. 一覧画面の絞り込み条件と検索パラメータを整理する。
3. 全文検索とタグ検索の API / 画面導線を作る。
4. トップ／一覧／詳細／検索結果の画面雛形を実装する。

---

この版は「情報ハブ」設計に沿ったロードマップです。詳細（検索アルゴリズム、認可設計、ETL 実装）を展開しましょうか？
