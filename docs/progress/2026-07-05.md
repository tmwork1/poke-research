# 進捗メモ 2026-07-05

## 今日の作業
- M3 の残タスク「個人ブログ（RSS）」の実現方式を検討した。
  - はてなブログ個別の RSS フィード自体は標準的に配信されており（`https://<blog>.hatenablog.com/rss`、RSS 2.0、`title`/`link`/`description`(本文込み)/`pubDate`/`guid`/`category`）、特定ブログを購読する方式なら技術的には問題なく実装できることを curl で確認した。
  - 一方で「ブログを個別に監視するのではなく、記事を定期的に横断検索したい」という要望が出たため、はてなブックマークの検索 RSS（`b.hatena.ne.jp/q/<keyword>?mode=rss`）を Qiita/Zenn/note と同様のキーワード検索型ソースとして使う案を検討した。
  - 最終的に、ToS が明確で公式ドキュメントのある Brave Search API を使う方針に決定した（はてなブックマーク検索はブックマークされた全ウェブが対象で、かつ非公式寄りの利用になるため）。
  - Brave Search API は API キーが必要な有料サービス（無料枠あり）のため、実装は今回のセッションでは行わず、リリース後のアップデート対応とし、ロードマップにその旨を明記した。M3 の完了条件からは外した。
- 上記決定に伴い、`docs/development-roadmap.md` の M3 セクションを更新し、Zenn/note の拡張が完了した状態で M3 を完了扱いとした。個人ブログ（Brave Search API 経由、リリース後対応）は将来対応として注記のみ残した。
- YouTube を収集対象にできないか検討したが、対象外と決定した。YouTube Data API v3（公式）で検索自体は可能だが、Qiita/Zenn/note のように記事本文をそのまま AI レビューに渡せる情報源がない（概要欄は短く宣伝目的のことが多く、字幕・文字起こしは任意の動画から確実に取得する手段がない＝OAuth 等の追加認証が必要になりうる）ため、収集対象からは外し、ロードマップに理由を明記した。
- UI からユーザーが記事 URL を投稿し、次回の定期取り込みから参照する仕組みを検討した。ブログのトップページ単位で監視する案も出たが、更新検知（RSS 自動検出等）が必要になり全ブログには対応できないため、記事そのものの URL に限定する方針に決めた。実装はリリース後のアップデート対応とし、今回はロードマップへの記載のみに留めた。

- M4「運用機能の追加」に着手した。想定ユーザーを「単一管理者 + 公開読み取り」と確認したうえで、認証方式は Supabase Auth / Cloudflare Access ではなく、`.env` の資格情報による Basic 認証を選択した（ローカル `astro dev` / `wrangler dev` / 本番のどこでも同じコードで検証でき、ログインUIやセッション管理の実装が不要なため）。
  - `src/lib/auth.ts` と `src/middleware.ts` を追加し、`/api/audit` を常時、それ以外の `/api/**` は書き込み系メソッド（GET/HEAD/OPTIONS 以外）のみ Basic 認証で保護した。閲覧系 API と `/api/export/items.csv` は引き続き公開のまま。
  - `migrations/003_add_audit_log.sql` で `audit_logs` テーブルを追加し、`src/lib/audit.ts`（`recordAuditLog`/`fetchAuditLogs`）を実装。`src/lib/db.ts` の共通 CRUD ヘルパー（`insertOne`/`updateOne`/`deleteOne`）に組み込み、items/sources/annotations の insert・update・delete を `table_name`/`record_id`/`action`/`actor`/`before`/`after` で自動記録するようにした。監査ログの記録失敗は本来の書き込みを止めず `console.error` に留める。
  - `GET /api/audit`（要認証、`table`/`recordId`/`limit` で絞り込み）を追加。items/sources/annotations の書き込み系 API ルート6ファイルに `locals.actor` を配線した。
  - `GET /api/export/items.csv`（認証不要）を追加し、`/api/items` と同じ q/kind/tag/sourceId フィルタで絞り込んだ items を UTF-8 BOM 付き CSV で出力できるようにした（`src/lib/csv.ts` を新規実装、外部ライブラリは追加していない）。
  - `scripts/test-db.mjs` に `audit_logs` の insert/delete スモークテストを追加。新テーブル追加に伴い `scripts/grant-dev-perms.mjs` を再実行して権限を付与した。
  - `astro dev --background` + curl で、認証無し書き込みが401になること、正しい Basic 認証で items の POST/PATCH/DELETE が成功しそれぞれ `/api/audit` に insert/update/delete の before/after が記録されること、CSV エクスポートが BOM 付きで正しいフィルタ結果を返すことを確認した。`npm run build` も成功。

- M5「検索条件の最適化」に着手した。OpenAI API課金を避けるため、実データ（Qiita 21件/Zenn 45件/note 6件、計72件）をClaude Codeが直接読んで評価した（AI採点スクリプトは書いていない）。
  - AIレビュー（`src/lib/importers/article-ai.ts`）の採用基準を検証したところ、72件中12件（17%）がポケモンと無関係、またはエッセイ的な内容にもかかわらず採用されていた（例: id23「GA4 Data API」、id28「Rubyのgem」、id36-40「Claude Fable 5」ニュース5本、id98「Vercel AI SDK」）。原因は、システムプロンプトが「ポケモンに言及していても技術性が無ければfalse」という条件しか持たず、「そもそもポケモンが主題でない場合はfalse」という明示的なルールが無かったこと。id98は現行（note対応で強化済み）プロンプトの下で収集されたものであり、今も再発しうる現在進行形の問題と確認した上で、システムプロンプトに「(1)主題がポケモン関連か → (2)技術的な実装・手法を扱うか」の2段階判定を明記する形に修正した。
  - 全文検索（`items.search_vector`、`to_tsvector('simple', ...)`）を実データで検証したところ、日本語が分かち書きされずCJK文が丸ごと1トークン化されるため、「ポケモン」で検索しても72件中3件しかヒットせず、「ポケモンカード」は関連記事が10件近くあるのに0件ヒットするなど、検索機能が実質機能していないことが判明した。`migrations/004_switch_search_to_trigram.sql` で `pg_trgm` 拡張を追加し、機能していなかった `search_vector` カラム・トリガー・インデックスを削除、タイトル・要約への trigram GIN インデックス + ILIKE 部分一致（複数語はAND）に切り替えた。適用後、`astro dev` 実データ経由の確認で「ポケモンカード」が8件、「ポケモン」が52件ヒットするようになった。
  - タグ名が大文字小文字で重複していた（「AI」4件/「ai」6件、「Python」3件/「python」4件など、332件中23件が重複）ため、`normalizeTagName`（`article-ai.ts`）を小文字統一に修正し、同じマイグレーションで既存タグの重複マージも実施した（309件に整理、`tag=ai` で10件ヒットすることを確認）。
  - `npm run build`、`scripts/test-db.mjs`、`astro dev --background` 経由の実APIリクエスト（`/api/items?q=...`、`/api/items?tag=...`）で修正後の挙動を確認した。
  - 一覧・詳細・検索結果画面のUI詳細設計は本セッションでは未着手。
- 上記の一回性の修正に続き、「検索精度」「フィルタ精度（AI取り込みレビュー）」「タグ精度」の3観点を独立した反復フローとして再構成した（`npm run eval:search` / `eval:filter` / `eval:tags`、詳細は [README](../../README.md)）。各フローで2巡目の評価・修正を実施した。
  - 検索精度: `eval-search.mjs` で複合語・別表記・タグ・kindなど12ケースを実データで再検証。「ポケモン ROM」のような複合AND検索は「ポケットモンスター」表記の記事を拾えないなど、substring一致特有の既知の限界はあるが、新規の欠陥は見つからず収束と判断した。
  - フィルタ精度: `eval-filter.mjs` で現行プロンプト＋収集済み72件を突き合わせ、Java/OWLの教材のように「ポケモンをドメインモデリングの題材として一貫して使っている記事」を主題該当として明示するルールを追加した（Fable5ニュースのような「一度触れるだけ」のケースとの区別を明確化）。
  - タグ精度: `eval-tags.mjs` で使用件数を集計したところ、220タグ中175件（79.5%）が使用1件のみという長い尾を発見した。`pokédex`/`pokedex`、`pokéapi`/`pokeapi`のアクセント表記違いの重複も発見し、`normalizeTagName`にアクセント除去を追加、`migrations/005_normalize_tag_diacritics.sql`で既存重複をマージした（307件に整理）。より根本的な対策として、収集時に使用頻度上位の既存タグをAIレビューへ渡し（`fetchTopTagNames`、`src/lib/importers/common.ts`）、類似タグの新規発行を減らすよう各インポーター（qiita/zenn/note）と`article-ai.ts`を修正した。この効果は次回以降の実収集でしか確認できない。
  - いずれも `npm run build` / `scripts/test-db.mjs` で検証した。
- ユーザーから「そもそも収集の入り口（Web検索）自体の精度は未検証では」と指摘を受け、4つ目の観点「収集クエリ精度（AIレビュー前の母集団のノイズ）」を追加した（`npm run eval:collection`、`scripts/eval-collection.mjs`）。DB保存済みの記事（=AIレビュー通過済み）ではなく、Qiita/Zenn/noteの検索・一覧APIが返す生の結果を直接叩き、タイトル一覧をClaude Codeが目視で判定する形にした。
  - Qiita（`query=ポケモン`、本文全文一致）: 200件中、体感50%前後が無関係なノイズと判定した。特に「今日のQiitaトレンド記事をポッドキャストで聴こう」という自動生成の日次まとめ投稿だけで15件以上を占め、他にも「Claude Fable 5」リリースニュース、Rubyのgem入門、AWS資格合格記など、本文中に「ポケモン」が一度でも登場するだけの記事が大量に混入していた。原因はQiita検索APIの`query`パラメータが本文全体を対象にした部分一致検索であること。
  - Zenn（`topic=pokemon`、トピックタグ絞り込み）: 69件中ノイズは数件程度で、もともと高精度だった。トピックタグによる絞り込みが自由文検索より本質的に精度が高いことを裏付けた。
  - note（`query=ポケモン`、本文全文一致）: 40件中、技術記事らしきものは体感0件。ほぼ全てが日記・エッセイ・観光記などで、ポケモン自体には関係するが技術実装とは無関係だった（現行AIレビューのルール2で弾かれる想定通りの内容だが、収集段階でのAIレビュー呼び出しがほぼ無駄になっている）。note側は検索クエリを工夫しても技術記事自体がほぼ存在しないため、母集団の性質の問題であり検索条件の問題ではないと判断し、対応を見送った。
  - Qiitaについて、`query=title:ポケモン OR title:ポケカ OR tag:ポケモン`（Qiitaの高度検索構文でタイトル・タグに絞る）を試したところ、100件中ノイズがほぼゼロになることを確認した（「ポケカ」「コイキング」「ピカチュウ」「600族」等の略称・固有名詞もタグ経由で正しく拾えている）。ユーザー確認のうえ、`src/lib/importers/qiita.ts`の`DEFAULT_QUERY`をこの絞り込みクエリに変更した。note側は同種の絞り込み構文をサポートするか未確認であり、母集団の性質上の問題（技術記事がほぼ無い）でもあるため今回は対応しなかった。
  - `npm run build`で検証した（このクエリ変更はDBスキーマに影響しないため`scripts/test-db.mjs`の対象外）。
- ユーザーから「検索ワードがハードコーディングされていて開発者が管理するのが困難」と指摘を受け、キーワード自体（「ポケモン」「ポケカ」等）はサイトによらず共通という前提で、`src/lib/importers/keywords.ts`に`POKEMON_KEYWORDS`として集約した。Qiita（`qiita.ts`）はこのリストから`title:X OR ... OR tag:先頭語`のクエリを組み立てる形に、note（`note.ts`）は既定クエリをリストの先頭語を参照する形に変更した（挙動はどちらも変更前と同じ値になることを確認済み）。Zenn は自然文キーワードではなく Zenn 独自のトピックタグ体系（`topic=pokemon`）のため対象外とした。
  - 併せて`.env.example`の`QIITA_QUERY=ポケモン`/`NOTE_QUERY=ポケモン`が非コメントで設定されていたため、`.env.example`をコピーしただけの環境ではコード側の既定値（改善後のクエリ）が黙って上書きされ、Qiitaの収集クエリのノイズ修正が無効化されてしまう状態だったことが判明した。両方コメントアウトし、コード側の既定値が有効になるようにした。
  - `npm run build`で検証した（DBスキーマに影響しないため`scripts/test-db.mjs`の対象外）。
- 続けて「Zennのトピックタグリストも同じ持ち方にできるか」と聞かれ、実際に他のポケモン関連トピックが存在するか`curl`で確認した。`pokemon`（48件、programming/AIコンペ系）とは別に`pokemongo`（10件、iOS/Androidのチートツール改造史エッセイ中心）が存在し、`pokemoncard`/`pokemon-tcg`は0件、日本語トピック名（`ポケモン`等）はZennのAPIエラーになる（トピックスラッグは英数字のみ）ことを確認した。ユーザー確認のうえ、仕組み（複数トピックのマージ・重複排除）だけ用意し、既定は`pokemon`単独のまま変更しない方針とした。
  - `src/lib/importers/keywords.ts`に`ZENN_TOPICS`（現状`['pokemon']`、`pokemongo`は候補としてコメントのみ）を追加。`zenn.ts`はカンマ区切りのトピック文字列を受け取り、`splitTopics`で分解して`fetchZennSlugs`がトピックごとに取得・スラッグ単位で重複排除するように変更した（`ZennSyncOptions.topic`や`ZENN_TOPIC`環境変数の型・意味は変更せず、複数指定時はカンマ区切りにするだけ）。
  - `.env.example`の`ZENN_TOPIC=pokemon`も、QIITA_QUERY/NOTE_QUERYと同様に非コメントで既定値を上書きしてしまう問題があったためコメントアウトした。`scripts/eval-collection.mjs`のZenn取得も同じマージ・重複排除ロジックに合わせて更新し、既定の`topic=pokemon`で69件と変更前と同じ件数が返ることを確認した。
  - `npm run build`で検証した（DBスキーマに影響しないため`scripts/test-db.mjs`の対象外）。
- ユーザーから「topicは質に直結するのでenv管理すべきでない」と指摘を受け、QIITA_QUERY/ZENN_TOPIC/NOTE_QUERYの3つとも同じ理由（収集内容の質を左右する設定であり、運用者が気軽にenvで変えられるべきではない）でenv経由の上書きを廃止する方針とした。
  - `resolveQiitaSyncOptions`/`resolveZennSyncOptions`/`resolveNoteSyncOptions`（各インポーター）から`env.QIITA_QUERY`/`env.ZENN_TOPIC`/`env.NOTE_QUERY`の参照を削除し、`QiitaEnvDefaults`/`ZennEnvDefaults`/`NoteEnvDefaults`からも該当フィールドを削除した。検索語・トピックは`src/lib/importers/keywords.ts`の値（`DEFAULT_QUERY`/`DEFAULT_TOPIC`）に一本化し、コードの変更（＝レビューを経た変更）でしか変えられないようにした。API経由の明示的な上書き（POSTボディの`query`/`topic`）はその場限りの手動実行向けとして残した。
  - `scripts/collect-qiita.mjs`/`collect-zenn.mjs`/`collect-note.mjs`が`process.env.QIITA_QUERY || 'ポケモン'`のようにenvから読んだ値を常にPOSTボディへ送っていたため、ライブラリ側からenvの参照を消しても、これらのスクリプト経由だと結局古いデフォルト値で上書きしてしまう抜け道が残っていた。3スクリプトとも、envが明示的に設定されている時だけ値を送り、未設定なら`undefined`にしてJSON.stringifyでキー自体を落とす（＝API側のコード既定値が使われる）ように修正した。
  - `.env.example`からQIITA_QUERY/ZENN_TOPIC/NOTE_QUERYの行を削除し、`astro.config.mjs`のenvスキーマからも未使用になった`QIITA_QUERY`を削除した。API route（`/api/import/qiita`,`/zenn`,`/note`）とREADMEのコメントも「環境変数から読む」という記述を「コード（keywords.ts）で管理する」に修正した。
  - `npm run build`で検証した（DBスキーマに影響しないため`scripts/test-db.mjs`の対象外）。

## 現在の全体状況
- M1: 完了
- M2: 完了
- M3: 完了
- M4: 完了（アクセス制御・監査ログ・CSVエクスポートを実装、詳細は上記参照）
- M5: 進行中（検索条件の最適化は完了。UI詳細設計は一覧/検索結果画面に着手。アイテム詳細画面は情報量が薄いため廃止を決定）

- M5「UI詳細設計」に着手し、一覧・検索結果画面を対象に壁打ちで仕様を固めたうえで実装した。
  - `/items`（一覧）と`/search`（検索結果）が同じ`CatalogPage`コンポーネント・同じクエリロジックで実質同一画面だったため、`/items`に一本化し、`/search`はq/tag/sourceId/pageを引き継いで`/items`へ301リダイレクトするだけの薄いページにした。
  - 種別（kind）フィルタを画面から削除した。Qiita/Zenn/noteいずれの収集経路も`kind='article'`固定で、library/github/paper/videoを選んでも常に0件になる死んだUIだったため。`fetchCatalogItems`側のkindフィルタ自体はAPI（`/api/items?kind=`)の後方互換のため残した。
  - タグフィルタを、307件を全部並べる`<select>`から、使用件数上位20件のクリック可能なチップ表示に変更した（`fetchTopTags`を`catalog.ts`に追加、`item_tags`を集計）。上位以外のタグはUIには出さないが、`tag`パラメータ自体は任意の値を引き続き受け付ける。
  - ページネーションを追加した（1ページ20件、ページ番号式）。`fetchCatalogItemsPage`（`catalog.ts`）でSupabaseの`count:'exact'`+`range()`を使い、総件数とページ数を返すようにした。
  - 一覧カードの種別バッジ（常にarticleで無意味だった）を廃止し、出典（Qiita/Zenn/note）を色分けしたバッジに置き換えた（`source.type`で判定）。
  - ユーザーから「明るいページで丸みのあるUIがいい。Qiitaのような」という指示を受け、ダーク基調のグラスモーフィズムからライト基調・丸みのあるQiita風の配色に全画面（トップ・一覧・詳細）を切り替えた。CSS変数（`--bg`/`--card-bg`/`--accent`など）を`Layout.astro`の`:root`に集約し、各ページのスタイルをそこから参照する形にした。アクセントカラーはQiitaグリーン（#55c500）を採用。
  - `npm run build`で検証し、`astro dev`起動後にChromeのheadlessスクリーンショット機能（`chrome --headless --screenshot`、Playwright等は未インストールのため）でトップ・一覧・詳細の3画面を実際に目視確認した。タグチップの選択状態、ページ送り（1/4表示）、出典バッジの色分け、ライトテーマでのコントラストに問題がないことを確認済み。
  - ユーザー指摘により、`/items`ヒーロー・トップページそれぞれにあった件数集計表示（Items/Tags/Sources/Annotations等）を削除した。トップページは表示に使っていなかった`fetchCatalogOverview`（tags/sources/annotationsも取得）をやめ、`fetchCatalogItems({limit:6})`だけを呼ぶ形に簡素化した。
  - ユーザーから「個別記事の表示がトップと一覧検索ページで異なる。一覧検索のほうが優れている」と指摘を受け、トップページが独自の簡易カード（kind表示・タグを文字列連結のみ）を持っていたことが原因と判明。カードのマークアップ・スタイルを`ItemCard.astro`として切り出し、`CatalogPage.astro`とトップページ（`index.astro`）の両方から使う形にして表示を統一した（出典バッジ・日付・タグチップ・詳細/外部リンクのボタンが両画面で同じになる）。
  - `npm run build`、および`astro dev`+Chrome headlessスクリーンショットでトップ・一覧の両画面のカード表示が一致していることを確認した。
  - ユーザーから「アイテムの最上部に(note)のように色付きで表示されるのがうっとうしい」と指摘を受け、`ItemCard`最上部の出典色分けバッジ（Qiita緑/Zenn青/note橙）を廃止した。出典名は外部リンクボタンのラベルに統合し、「外部リンク」から「noteで記事を見る」のような表記に変更した（`item.source.name`が無い場合は「記事を見る」）。
  - ユーザーから「`詳細を見る`リンクの情報が薄い」「詳細ページ自体の中身も薄い（カードと重複するタイトル/要約/タグに加え、注釈・メタデータは生JSONダンプのみ）」と指摘を受け、壁打ちの結果アイテム詳細ページ（`/items/[id]`）自体を廃止することにした。`src/pages/items/[id].astro`を削除し、`ItemCard`のカードタイトルを外部リンク（`item.external_url`、新規タブ）へ直接張るように変更、`詳細を見る`リンクと出典名入り外部リンクボタンは両方削除した（出典名はメタ行にプレーンテキストで表示）。`GET/PUT/PATCH /api/items/[id]`は詳細ページと無関係のAPIのため維持している。`/items`ページのリード文（「各アイテムの詳細へ移動できます」）も実態に合わせて修正した。
  - 上記に伴い、詳細画面の「注釈」「メタデータ」の生JSON表示の可読化という課題自体が解消（対象画面が存在しなくなったため）。今後仮に注釈情報をユーザーに見せたくなった場合は、詳細ページ復活ではなくカード上での要約表示などを検討する。
  - ユーザーから「shortcutsもいらない」と指摘を受け、トップページの`Shortcuts`（一覧・検索画面/API items/API tagsへのリンク一覧）と、隣接していた`Next step`（種別フィルタ等の説明文。既に廃止済みの種別フィルタに言及しており内容も古くなっていた）の2カラムパネルを丸ごと削除した。合わせてヒーロー文言の「検索と詳細確認をそのまま往復できる」という詳細ページ前提の表現も実態に合わせて修正した。
  - 続けてユーザーから「M2 search and uiのブロックも削除」と指摘を受け、ヒーロー見出し上の内部マイルストーン名（`M2 Search and UI`のeyebrowラベル）をエンドユーザーに無関係な内部情報として削除した。
  - 最終的にユーザーから「知見を集め〜一覧・検索へ、が丸ごと不要」と指摘を受け、トップページのヒーロー見出し（h1見出し・説明文・一覧/検索への導線ボタン）自体を丸ごと削除した。トップページは`最近追加されたアイテム`パネルのみのシンプルな構成になった（`/items`への導線はナビゲーションの`Items`リンクとパネル内の`すべて見る`リンクで足りるため）。未使用になった`.hero`/`.hero-links`関連CSSとメディアクエリの参照も削除した。
  - ユーザーから「日付は元記事の執筆日で管理すべき」と指摘を受け、カード表示日付とアイテムの並び順を、こちらの取り込み時刻（`created_at`）ではなく元記事の公開日（`published_at`、Qiita/Zenn/noteいずれのインポーターも取得元APIの公開日をそのままセットしている）基準に変更した。`ItemCard`の日付表示は`item.published_at ?? item.created_at`にフォールバックし、`catalog.ts`の`queryCatalogItems`は`.order('published_at', {ascending:false, nullsFirst:false}).order('created_at', {ascending:false})`として、`published_at`が無いレコード（手動API投入時など）は末尾かつ`created_at`降順で並ぶようにした。並び順の変化（実際の記事公開日順になったこと）をスクリーンショットで確認済み。
  - ユーザーから「"Information Hub"削除」と指摘を受け、`Layout.astro`のヘッダー内、ブランド名（`Pokemon Research Hub`）上にあった`Information Hub`のeyebrowラベルを削除した。未使用になった`.eyebrow`ルールも同ファイルから削除した（他ページの`.eyebrow`はコンポーネントごとのスコープ付きスタイルのため無関係）。
  - ここまでの一連のトップページ・詳細ページ整理を踏まえ、ユーザーから「一覧検索ページも同様に磨いて」と指摘を受け、`CatalogPage.astro`（`/items`）にも同じ方針（低情報の装飾は全廃、フォームや選択チップと重複する情報は消す）を適用した。
    - ヒーロー見出し上の`Browse and search`eyebrowラベルと、フォームの内容をそのまま言い換えているだけのリード文（`lead` prop）を削除し、`<h1>`のみを残した。`CatalogPage`の`lead` propは唯一の呼び出し元である`items/index.astro`ごと削除した。
    - 「現在の条件: キーワード「〜」/ タグ 〜 / 公開元 〜（N件）」という条件再掲パラグラフ（`.active-filters`）を削除した。キーワード欄・公開元セレクトには選択値がそのまま表示され、タグは該当チップが`active`でハイライトされるため、同じ情報をテキストで繰り返しているだけだった。
    - 未使用になった`hasFilters`変数と`.hero`/`.eyebrow`/`.lead`/`.active-filters`関連CSSを削除した。
    - `npm run build`と`astro dev`+Chrome headlessスクリーンショットで`/items`の表示を確認した。フォーム・タグチップ・カード一覧・ページネーションは変更前と同じ挙動で、見出し直下の冗長なテキストのみが消えたことを確認済み。

- M6「リリース準備と安定化」に着手した。着手前にユーザーへ3点確認し、方針を決めた: (1) CI/CDの検証範囲は「build + 使い捨てPostgresでのマイグレーション検証」まで（本番Supabaseの資格情報はCIに置かない）、(2) バックアップ・復旧はSupabase標準バックアップ機能に依拠し独自スクリプトは作らない、(3) `wrangler deploy`によるデプロイ自動化はCI/CDに含めず引き続き手動。
  - `.github/workflows/ci.yml`を新設した。`build`ジョブは`npm ci`+`npm run build`。`migrations`ジョブはCIジョブ内で`postgres:16`サービスコンテナ（使い捨て）を起動し、`npm run migrate`で`migrations/*.sql`を空のスキーマに最初から順に適用できるか検証する。本番Supabaseの接続情報はCIに一切置かない。ローカルでも同じ内容をDocker（`postgres:16`コンテナ）で実行し、5本のマイグレーション（001〜005、`pgcrypto`/`unaccent`/`pg_trgm`拡張を含む）が空DBに最初から順に適用できることと`npm run build`が通ることを確認した。
  - `docs/operations.md`を新設し、CIの説明、デプロイ手順（本番`DATABASE_URL`での`npm run migrate`実行→`scripts/db/test-db.mjs`によるスモークテスト→`wrangler secret put`でのシークレット確認→`npm run deploy`→動作確認、の順）、バックアップ・復旧手順（Supabaseダッシュボードの`Database → Backups`からの復元→未適用マイグレーションの適用→`test-db.mjs`確認→`GET /api/audit`での復元直前の操作確認→新規プロジェクトに復元した場合のシークレット差し替え）、障害対応の初動（`wrangler tail`でのcronジョブ失敗確認、シークレット関連の500/401の切り分け）をまとめた。
  - README.mdに「CI」の節を追加し、GitHub Actionsが何を検証するか、デプロイ・バックアップ・障害対応は`docs/operations.md`を参照する旨を明記した。
  - `docs/development-roadmap.md`のM6の3項目（CI/CD自動化・バックアップ復旧手順・README/運用メモ整備）を完了として記録した。

- ユーザーから「デプロイに必要な操作を単一のスクリプトにまとめられないか」と依頼を受けた。`scripts/db/test-db.mjs`のスモークテスト（本番にテスト行をinsert→deleteする）を毎回自動実行するかを確認したところ、スキーマ変更時のみ必要な任意手順であり毎回本番に書き込みたくないとの回答だったため対象外とし、`scripts/release.mjs`を新設して「マイグレーション適用（`npm run migrate`）→デプロイ（`npm run deploy`）」の2ステップのみをまとめた（`npm run release`、`DATABASE_URL`必須）。マイグレーションが失敗した場合は`execSync`が例外を投げて処理が止まり、デプロイには進まない。
  - `DATABASE_URL`未設定時に即座に終了すること、マイグレーション失敗時にデプロイへ進まないこと（到達不能なDBを指定して確認）を実際に実行して検証した。実際の本番デプロイ（`wrangler deploy`）はこのセッションでは実行していない。
  - `docs/operations.md`のデプロイ手順を、マイグレーション適用とデプロイの2手順を`npm run release`一つに統合する形に更新した。

- ユーザーからCloudflare WorkersプロジェクトとGitHub repoを連携済みとの報告を受け、デプロイ手順を見直した。連携内容を確認したところ、`master`ブランチへのpush（PRのmergeを含む）でCloudflare側が自動でビルド・デプロイを行う設定だった。
  - `scripts/release.mjs`から`npm run deploy`（`wrangler deploy`）ステップを削除し、本番Supabaseへのマイグレーション適用（`npm run migrate`）のみを行うスクリプトに変更した。アプリのビルド・デプロイ自体はCloudflareの自動デプロイに委ねる。
  - `docs/operations.md`のデプロイ手順を、「マイグレーション適用（`npm run release`）→`master`へpush/merge→Cloudflareが自動デプロイ→動作確認」の順に更新した。`wrangler deploy`（`npm run deploy`）自体は削除せず、Cloudflare連携が使えない緊急時やmasterを経由しない検証用の手動デプロイ手段として残した。
  - README.mdのCI節と`docs/development-roadmap.md`のM6の記述も、デプロイがCloudflareのGit連携により自動化されたことに合わせて更新した。
  - `DATABASE_URL`未設定時に即終了することを実際に実行して再確認した。実際の本番マイグレーション適用・pushはこのセッションでは行っていない。

## 現在の全体状況（更新）
- M6: 完了（CI/CD自動化・バックアップ復旧手順の文書化・README/運用メモ整備・Cloudflare Git連携によるデプロイ自動化・本番マイグレーション適用は`npm run release`に統合。マイグレーション適用自体は方針により引き続き手動）

- ユーザーの依頼で、`npm run release`を使い本番Supabaseへ初めてマイグレーションを適用した。
  - 当初`DATABASE_URL`のパスワード誤りで認証エラーになり、ユーザーに訂正してもらった。訂正後のパスワードに未エンコードの`/`が含まれ`Invalid URL`エラーになったため、`%2F`にURLエンコードして解決した。
  - migration 001実行時に`items_source_id_fkey`の型不一致（`integer` vs `uuid`）で失敗。調査したところ、本番Supabaseプロジェクトの`public`スキーマに現行アプリと無関係な初期プロトタイプの残骸テーブル（`articles`/`authors`/`sources`(uuid)/`article_tags`/`ingest_jobs`、計6テーブル）が既に存在していたことが原因と判明。ユーザーに確認のうえ不要と確定し、既存の`scripts/db/drop-non-migrations-tables.mjs --dry-run`でプレビュー後、承認を得てから実削除した。以降`npm run release`で001〜005が本番に正常に適用された。
- `scripts/db/test-db.mjs`を本番`SUPABASE_URL`/キーで実行したところ、レガシー形式（JWT）の`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_KEY`が`PGRST301`（JWTデコード不可）で認証エラーになった。ダッシュボードから再取得しても同じ値で変わらず、ユーザー確認の結果「Legacy API keys」を無効化するトグル自体が存在しないプロジェクトと判明。最終的にユーザーの判断で新形式キー（`sb_publishable_...`/`sb_secret_...`）に切り替え、コード変更なしでスモークテストが成功した（`createClient`はキー形式を問わない）。
  - 新形式への切り替えに合わせ、コードベース全体で`SUPABASE_ANON_KEY`→`SUPABASE_PUBLISHABLE_KEY`、`SUPABASE_SERVICE_KEY`→`SUPABASE_SECRET_KEY`にリネームした（`astro.config.mjs`/`src/lib/supabase.ts`/`scripts/db/test-db.mjs`/`scripts/db/setup-env.ps1`/`.env.example`/`docs/operations.md`/`.env`/`.dev.vars`）。値は変更せず名前のみ。`npm run build`と本番スモークテストで確認済み。
- 本番Cloudflare Workerに`wrangler secret put`でシークレットを設定したところ、`SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_SECRET_KEY`だけでなく`ADMIN_USERNAME`/`ADMIN_PASSWORD`/`OPENAI_API_KEY`/`OPENAI_MODEL`も未設定だったことが`wrangler secret list`で判明し、全7件を設定した。`.env`の`ADMIN_PASSWORD`が開発用の仮値（`dev-password`）のままだったため、ランダムな強いパスワードを生成して`.env`と本番シークレットの両方を更新した。
  - シークレット設定後、`curl`で本番`GET /api/items`を叩いたところ500エラー。`wrangler tail`で調査した結果、シェル環境で`grep '^KEY=' .env | cut -d= -f2- | wrangler secret put KEY`というパイプを使っていたことが原因と判明：（1）`.env`にUTF-8 BOMが混入していたこと自体は影響しなかったものの、（2）このシェルの`grep`が要約行（`1 matches in 1 files:`）を出力に付与しており、それが`cut`を素通りして`wrangler secret put`に渡り、設定した7件のシークレット全てが汚染されていた。`.env`をBOM無しで再書き込みし、以降はNode（`fs.readFileSync`でパースして`process.stdout.write`）でパイプを組み、`grep`/`ls`を経由しない方法で7件を再設定した。`GET /api/items`が200、Basic認証付き`GET /api/audit`が資格情報あり200・なし401になることを確認し、本番が正常に動作していることを確認した。

- M7「Googleログインとマイページ」に着手した。着手前に、作業ツリーに残っていた未コミットの変更（ブランチ名master→main統一、本番ドメイン`poke-research.com`の反映、ブランド名`PokeResearch`化）を先にコミットしてから進めた。`docs/spec-mypage-auth.md`の設計に従い、以下を実装した。
  - `@supabase/ssr`を追加し、`src/lib/user-session.ts`でCookieベースのセッション用Supabaseクライアント（`createServerClient`、Astroの`request`/`cookies`をgetAll/setAllで橋渡し）と`getSessionUser`を実装した。
  - `migrations/006_link_users_to_auth.sql`で`public.users.id`を`auth.users(id)`参照に変更し、`auth.users`へのINSERT時に`public.users`へ自動反映するトリガー（`handle_new_auth_user`、`display_name`はGoogleの`full_name`/`name`から取得）を追加した。CIの`postgres:16`には`auth`スキーマが無いため、`.github/workflows/ci.yml`の`migrations`ジョブに最小限の`auth.users`スタブテーブルを作るステップを追加した。Docker上で001〜007を空DBに適用できること、`auth.users`へのINSERTで`public.users`にトリガーが反映されること、`bookmarks`の外部キーが正しく効くことを実際に確認した。
  - `migrations/007_add_bookmarks.sql`で`bookmarks`テーブル（`user_id`/`item_id`複合主キー）を追加した。
  - `src/middleware.ts`を拡張し、全リクエストで`locals.user`にセッションを読み込むようにした。`/api/auth/**`は管理者Basic認証の対象から除外し、`/api/bookmarks`は「`locals.user`が存在すること」を要求する別レーンとして扱う（既存の管理者向け書き込み保護ロジックは変更していない）。
  - `src/pages/api/auth/login.ts`（Google認可URLへリダイレクト）・`callback.ts`（`exchangeCodeForSession`→`/mypage`へリダイレクト）・`logout.ts`（`signOut`→`/`へリダイレクト）を実装した。
  - `src/lib/bookmarks.ts`（追加はupsert+`ignoreDuplicates`で冪等に、削除はDELETE）と、`src/lib/catalog.ts`に`fetchBookmarkedItemIds`/`fetchBookmarkedItems`を追加し、`GET/POST /api/bookmarks`・`DELETE /api/bookmarks/:itemId`を実装した。いずれも`locals.user.id`基準でしか操作できない。
  - `src/pages/mypage.astro`を追加した。未ログイン時はログインボタンのみ、ログイン時は表示名・メール・ログアウトボタンとお気に入り一覧（`ItemCard`再利用）を表示する。`Layout.astro`のヘッダーナビにマイページへのアイコンリンクを追加した。
  - `ItemCard.astro`にお気に入りトグルボタン（アイコンのみ、色付きバッジは使わない）を追加し、クリック時に`fetch`で`/api/bookmarks`へPOST/DELETEする。未ログイン時は`/api/auth/login`へ遷移する。トップページ・`/items`・`/mypage`それぞれから、ログイン状態とお気に入り済みitem_idの集合を渡すように配線した。
  - `npm run build`で型検証、`astro dev`（本番Supabase接続）で`/`・`/items`・`/mypage`（未ログイン時のログインボタン表示）・`/api/bookmarks`の未ログイン401・`/api/auth/login`のリダイレクト（管理者Basic認証の対象外であること）・`/api/items`のGET公開/POST401（既存の管理者保護が壊れていないこと）を確認した。
  - この過程で、`.dev.vars`（wrangler/`cloudflare:workers`の`env`が読む値）が本番Supabaseキー移行前の古いローカルエミュレータ用の値（`SUPABASE_URL=http://localhost:54321`など）のまま取り残されており、`.env`（Viteの`process.env`側）とズレていたことが判明した。ローカル`astro dev`向けのSupabase設定は本来同じ値であるべきのため、`.dev.vars`を`.env`の内容で上書きして同期した（両ファイルとも`.gitignore`済みでコミット対象外）。
  - `/api/auth/login`が実際に本番SupabaseプロジェクトのGoogle認可URL（`https://<project>.supabase.co/auth/v1/authorize?provider=google&...`）へ302リダイレクトすること、PKCEのcode_verifier Cookieが発行されることを確認した。ただしSupabaseダッシュボードでGoogleプロバイダがまだ有効化されていないため、認可URLへの実際のアクセスは`validation_failed`エラーになる（想定通り、ユーザー側の設定待ち）。
  - 未実施: (1) Google Cloud ConsoleでのOAuthクライアント作成とSupabaseダッシュボードでのGoogleプロバイダ有効化（ユーザー操作が必要）、(2) `npm run release`による本番マイグレーション006/007の適用、(3) 本番環境での実際のログイン→`/mypage`→お気に入り追加/削除のエンドツーエンド確認。
- ユーザーがGoogle Cloud ConsoleでOAuthクライアントID（ローカル用・本番用の2つ）を作成した。ローカル開発中もGoogleログインを試したいという意図。
  - ユーザーが`.dev.vars`をローカルSupabase CLIスタック（`http://127.0.0.1:54321`、Docker、33時間起動していた既存スタック）向けの値に差し替えた。調査の結果、このローカルスタックは本リポジトリではなく`C:\Users\tmtmp\Documents\test\supabase\`（`project_id = "supabase"`、gitリポジトリではない汎用スタック）の`supabase/config.toml`で管理されていることが判明した。ローカルDBには当時migration 001〜005相当のテーブルのみ存在し、006（authユーザー連携）・007（bookmarks）は未適用だった。
  - `DATABASE_URL`を一時的に`postgresql://postgres:postgres@127.0.0.1:54322/postgres`に指定して`scripts/db/run-migrations.mjs`をローカルDBに対して実行し、006・007を適用した（`.env`は変更していない）。
  - ユーザー了承のうえ、`test/supabase/supabase/config.toml`に`[auth.external.google]`を追加（`enabled = true`、`client_id`はインライン、`secret = "env(SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET)"`）し、同ディレクトリに`supabase/.env`（新規、gitなし）を作成して`SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET`を設定した。ローカル用Client ID: `659456349626-l8jdd7sqal56kc7m76ho79f8d1hrs4bq.apps.googleusercontent.com`。
    - やり取りの過程で、ユーザーが誤って本番Supabaseの`DATABASE_URL`パスワード（`7_Uj/QqEvAJD-Sc`）をGoogle Client Secretとして貼り付ける場面があった。会話に露出したため、本番DBパスワードのローテーションをユーザーに推奨した（本人が対応要）。
  - `npx supabase stop` → `npx supabase start`（`test/supabase`ディレクトリで実行）でスタックを再起動し、`docker inspect supabase_auth_supabase`で`GOTRUE_EXTERNAL_GOOGLE_ENABLED=true`など反映済み・`GOTRUE_EXTERNAL_GOOGLE_REDIRECT_URI=http://127.0.0.1:54321/auth/v1/callback`が自動導出されていることを確認した。DBはボリュームから復元され、bookmarksテーブルを含む006/007の内容も保持されていた。
  - `astro dev --background`で`poke-research`のローカルサーバーを起動し、`GET /api/auth/login`が`http://127.0.0.1:54321/auth/v1/authorize?provider=google&...`へ302、続けてそのURLが`https://accounts.google.com/o/oauth2/v2/auth?client_id=659456349626-l8jdd7sqal56kc7m76ho79f8d1hrs4bq...&redirect_uri=http%3A%2F%2F127.0.0.1%3A54321%2Fauth%2Fv1%2Fcallback&...`へ302することを`curl`で確認した。PKCEのcode_verifier Cookieも発行された。
  - 未確認: Google Cloud Console側の「承認済みのリダイレクトURI」に`http://127.0.0.1:54321/auth/v1/callback`が登録されているかはユーザー側の確認待ち。登録されていれば、ブラウザでの実ログイン→`/mypage`→お気に入り操作まで通しで確認できるはず（未実施）。
  - その後、ローカル用Client ID/Secretを`659456349626-virai3mccf13vb7bp8h7crrb82c0049n.apps.googleusercontent.com`に差し替えた（`test/supabase/supabase/config.toml`・同ディレクトリの`supabase/.env`を更新→`npx supabase stop`/`start`で再起動）。`docker inspect`で`GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID`が新IDに更新されたこと、`curl`で`/api/auth/login`→Supabase authorize→Google認可URLまで新Client IDで正しくリダイレクトされることを確認した。
  - 副次的な発見: `astro dev`＋`@astrojs/cloudflare`アダプタの組み合わせで、サーバー側で例外が発生すると、astroの標準ロガー自体が`process is not defined`で例外を握りつぶし、本来のエラーメッセージがコンソールにもエラーオーバーレイにも出ない（`nodejs_compat`未設定のためworkerdランタイムに`process`が無いことが原因）。デバッグ時は該当ルートに一時的な`try/catch`を仕込んでエラー内容を直接レスポンスに含める必要があった（本件解決後は元に戻した）。本番用のグローバル対応は未検討・未実施。

- ユーザーから「ローカル開発ではGoogle認証せずにマイページを表示し、本番環境だけ認証を通すようにしたい」と依頼を受けた。ローカル用Google OAuthクライアントや別リポジトリのSupabase CLIスタックを都度立てて検証する運用が重かったため、方針を転換した。
  - `src/lib/user-session.ts`の`getSessionUser`に、`import.meta.env.DEV`が真のとき（`astro dev`実行時のみ）固定のダミーセッションユーザー（`DEV_SESSION_USER`、id`00000000-0000-0000-0000-000000000001`）を無条件に返す分岐を追加した。Supabase Auth自体を呼ばないため、ローカルではGoogleログインを経由せず`/mypage`が常にログイン済み表示になる。`astro build`（本番相当）では`import.meta.env.DEV`が静的に`false`になり、この分岐はビルド後の`dist`から完全に除去されること（`grep`でdist内に`DEV_SESSION_USER`が存在しないことを確認）、`getSessionUser`が実際にSupabase Authを呼ぶ経路のみ残ることを確認済み。
  - お気に入り（bookmarks）機能もこのダミーユーザーで実データに対して動作検証できるよう、`scripts/db/seed-dev-user.mjs`を新設した。同じidで`auth.users`にINSERT（`ON CONFLICT DO NOTHING`）し、`public.users`にも明示的にINSERTする（`migrations/006`のトリガー導入前にauth.users側だけ先に存在した場合でも確実に整合させるため）。
  - この過程で、本番Supabaseに`migrations/006_link_users_to_auth.sql`/`007_add_bookmarks.sql`が実はまだ未適用（`migrations`テーブルは005まで）だったことが判明した。ユーザー確認のうえ`npm run release`で本番に006/007を適用した。適用前は本番`items`テーブルが0件だったことも判明し（M1〜M6の収集・評価作業はこれまで本番以外のDBに対して行われていたと判明）、これは今回のスコープ外として現状のまま記録に留めた。
  - ローカル用Google OAuthを試す用途が無くなったため、`.dev.vars`（ローカル`astro dev`が読むSupabase接続情報）を`.env`（本番Supabase）の内容に同期した。以降ローカル開発は本番Supabaseに対して行う（bookmarksの追加・削除も本番DBに書き込まれる点に注意）。ローカル専用Google OAuthクライアントや別ディレクトリ（`C:\Users\tmtmp\Documents\test\supabase\`）のSupabase CLI Dockerスタックは今回の変更後は不要になったが、削除・停止はユーザー操作に委ねた（このリポジトリの管理外のため）。
  - `npm run build`、`astro dev --background`起動後の`curl`で、`/mypage`が未ログイン導線を経ずに`ローカル開発ユーザー`表示になること、本番`items`に一時テストレコードを1件作成して`POST/GET/DELETE /api/bookmarks`が実際に成功すること（追加→一覧反映→削除→一覧から消える）を確認し、テストレコードは削除して後片付けした。

- ユーザーから「本番環境でGoogle認証できるか確認したい」と依頼を受け、上記変更を`main`へpush（Cloudflare Git連携により自動デプロイ）した。
  - GitHub Actions CIの`build`ジョブが失敗したが、直前のコミット（今回の変更前）でも同じ`node:module`の`registerHooks`未エクスポートエラーで落ちていることを確認し、今回の変更由来ではない既存の問題（CIランナーのNodeバージョンがpackage.jsonの`engines`要件`>=22.12.0`を満たしていない可能性）と判断した。Cloudflare側のデプロイチェック（`Workers Builds: poke-research`）は成功しており、デプロイ自体は完了している。
  - `curl`で本番`/mypage`が未ログイン時にログインボタン表示のままであること（ローカル開発用のダミーユーザーバイパスが本番に漏れていないこと）、`GET /api/auth/login`が本番Supabaseの認可URLへ302すること、そのURLがGoogleの`accounts.google.com/o/oauth2/v2/auth`（本番用Client ID）へさらに302し、`redirect_uri_mismatch`等のエラーにならず実際のGoogleサインイン画面（title: "Sign in - Google Accounts"）まで到達することを確認した。サーバーサイドの設定（Supabase Google プロバイダ有効化・本番OAuthクライアントのリダイレクトURI登録）は機能している。
  - ブラウザでの実際のGoogleアカウント入力・同意・`/api/auth/callback`経由での`/mypage`表示・お気に入り追加までは、実際のGoogle資格情報が必要なためユーザー自身による確認が必要（未実施）。

## 現在の全体状況（更新）
- M7: 実装済み・本番デプロイ済み。ローカル開発はGoogle認証をバイパスしてダミーユーザーで検証、本番のみ実際のGoogleログインを要求する構成。本番マイグレーション006/007は適用済み。サーバーサイドのGoogle OAuth連携（Supabase authorize→Google同意画面）は`curl`で疎通確認済み。未実施: ブラウザでの実際のログイン→`/mypage`→お気に入り追加/削除のエンドツーエンド確認（ユーザー操作待ち）。CI（GitHub Actions）の`build`ジョブが今回と無関係の既存原因（Node バージョン起因とみられる`registerHooks`エラー）で落ちている点は別途対応が必要。

- ユーザーがブラウザで本番Googleログインを試したところ、2段階のエラーで失敗した。いずれもダッシュボード側の設定不備で、コード変更は不要だった。
  - 1段階目: Google側で`エラー 400: redirect_uri_mismatch`。原因はGoogle Cloud Consoleの本番用OAuthクライアントの「承認済みのリダイレクトURI」に、SupabaseのGoogleプロバイダーが固定で使う`https://idiiakuduastlqcusdip.supabase.co/auth/v1/callback`が未登録だったこと。ユーザーが登録して解消した。
  - 2段階目: Google認証自体は成功するが、`http://localhost:3000/?code=...`へ遷移して失敗。原因はSupabaseダッシュボード（Authentication → URL Configuration）の**Redirect URLs**に本番コールバック`https://poke-research.com/api/auth/callback`が登録されておらず、許可リスト照合に失敗して**Site URL**のデフォルト値（`http://localhost:3000`）にフォールバックしていたこと。ユーザーがRedirect URLsに本番コールバックを追加し、Site URLも`https://poke-research.com`に変更して解消した。
  - 上記2件の解消後、ユーザーから本番ログイン成功の報告を受けた（「解決！」）。ブラウザでの実際のログイン→`/mypage`表示→お気に入り追加/削除のエンドツーエンド確認が完了したことになる。

- ユーザーから「ローカルサイトにアイテムが一件も表示されない」と報告を受けた。調査の結果、直前のセッションで`.dev.vars`/`.env`を本番Supabaseの接続情報に同期していたが、本番`items`テーブルは元々0件（M1〜M6の収集データは`C:\Users\tmtmp\Documents\test\supabase\`のローカルSupabase CLIスタックにのみ存在）だったことが原因と判明した。ユーザー確認のうえ、本番に対して収集をやり直すのではなく、ローカル開発を再度このローカルスタック（72件のitemsを保持）に向ける方針とした。
  - `.env`/`.dev.vars`の`SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_SECRET_KEY`/`DATABASE_URL`をローカルスタック（`http://127.0.0.1:54321`、`postgresql://postgres:postgres@127.0.0.1:54322/postgres`）の値に戻した（`ADMIN_*`/`OPENAI_*`はDB接続と無関係のため変更なし）。
  - `astro dev`再起動後、トップ・`/items`ではデータが表示されたが、ブックマーク関連で`permission denied for table bookmarks`エラーが発生。ローカルスタックが再起動された際に`scripts/db/grant-dev-perms.mjs`のGRANTが効いていなかったためと判明し、`DATABASE_URL`をローカル用に指定して再実行し解消した。
  - `public.users`/`auth.users`にもダミーユーザー（`00000000-0000-0000-0000-000000000001`）が未登録だったため`scripts/db/seed-dev-user.mjs`を再実行し、`POST/GET/DELETE /api/bookmarks`が成功することを確認した。
  - 本番Supabaseの`items`が空である問題自体（本番向けの記事収集の実行）は今回のスコープ外のまま。

- ユーザーから`docs/plan/ui_improve_plan_20260705.md`（HOME/Search/My pageのUI改善要望リスト）の実施を依頼された。着手前に、AIツールチップの対象範囲（全タグ/難しい語のみ）・おすすめ記事のロジック（人気のみ/ブックマーク類似度/ハイブリッド）・ログイン状態表示（アイコンの見た目変化/アバター画像）の3点をヒアリングし、「難しい語のみ」「ブックマークタグ類似度×人気のハイブリッド」「アイコンの見た目変化（アバターなし）」で合意して実装した。
  - HOME: `ItemCard.astro`のmeta-rowに著者名（`item.authors`）を日付とソースの間へ追加。ブックマークボタンの隣に件数表示を追加し（`catalog.ts`の新規`fetchBookmarkCounts`、`bookmarks`テーブルをJS側で集計）、クリック時に楽観的に±1する。
  - HOME: タグが専門用語の場合だけAIが解説を出すツールチップを実装した。`migrations/008_add_tag_explanations.sql`で`tags`に`is_difficult`/`explanation`/`explained_at`を追加し、初回ホバー時に`GET /api/tags/:id/explain`→`src/lib/tag-explain.ts`がOpenAIへ「難しい語か・難しければ1〜2文で解説」を問い合わせて`tags`にキャッシュする（2回目以降はDBの値を返しAIを呼ばない）。OpenAI呼び出しの共通設定は`src/lib/importers/article-ai.ts`から`src/lib/openai.ts`に切り出して両方から使う形にした。難しい判定のタグだけ`title`属性に解説文をセットし、ブラウザ標準のツールチップで表示する。
  - Search: ページ送りを`Pagination.astro`として切り出し、`CatalogPage.astro`の一覧の上下両方に配置した。
  - Search: `Layout.astro`のヘッダーに虫眼鏡アイコン（`/items`へのリンク）を追加した。
  - My page: ログインボタンをGoogle公式風の白背景・4色ロゴ入りボタンに変更した。
  - My page: ヘッダーのマイページアイコンに`loggedIn`状態を反映し、ログイン中はアクセントカラーで塗りつぶす見た目に変えた（`Layout`に`loggedIn` propを追加、`index.astro`/`items/index.astro`/`mypage.astro`から配線）。
  - My page: `catalog.ts`に`fetchRecommendedItems`を追加し、ブックマーク済みアイテムのタグ集合との重なり×人気（ブックマーク数）のスコアでおすすめ記事を算出（ブックマークが無ければ自然に人気順にフォールバック）。「お気に入り」の下に「おすすめ記事」パネルとして表示した。
  - 検証: `npm run migrate`（ローカルSupabaseスタック、008を適用）、`npm run build`、`astro dev`起動中のローカルサーバーへ`curl`で実際にHOME/`/items`/`/mypage`のHTMLと`/api/tags/:id/explain`（初回はOpenAI呼び出しで解説生成・2回目はキャッシュから0.17秒で返却）を確認した。著者名・ブックマーク数・タグの`data-tag-id`・上下ページ送り・検索アイコン・Googleボタン・ログイン状態アイコン・おすすめ記事セクション、いずれも意図通り描画されることを確認済み。

- ユーザーが`docs/plan/ui_improve_plan_20260705.md`に自分で項目を追記し（ナビゲーション/記事レイアウト/HOME/Search/My page/発見性・SEO/人気度・ランキング/共有/開発者コンタクト）、実施を依頼された。着手前に、おすすめ記事のフィルタ・ソート軸（タグ絞り込みのみ/並び替えのみ/両方）とログアウトボタンの変更内容（パネル内で控えめに/ヘッダー常設）の2点をヒアリングし、「タグ絞り込み+並び替えの両方」「パネル内で控えめに」で合意して実装した。
  - ナビゲーション: `Layout.astro`で`Astro.url.pathname`と各リンクの`href`を比較し、現在地のアイコンに`current`クラス（`opacity: 0.45`）を付与した。
  - 記事カード: `ItemCard.astro`の`meta-row`から日付を外し、タグ一覧の行に右寄せで統合(`.tag-row`)して1行分の縦の高さを削減した。
  - HOME: 見出し「最近追加されたアイテム」を削除し、`CatalogPage.astro`のタグクラウド部分を`TagCloud.astro`として切り出して、HOMEでも同じタグチップによる絞り込み(`?tag=`)を使えるようにした。
  - Search: 見出し「アイテム一覧・検索」を削除し、`sort`(新着順/人気順)の`<select>`を追加した。人気順は集計用ビューを持たないため、`catalog.ts`の`fetchCatalogItemsPage`で直近最大300件の候補プールを取得し`fetchBookmarkCounts`でJS側ソート・ページングする方式にした(この項目で「人気度・ランキング」の要望も兼ねる)。
  - My page: ログアウトボタンを小さく・低コントラストな見た目に変更(位置は変更なし)。`catalog.ts`の`fetchRecommendedItems`をタグ絞り込み・並び替え(おすすめ順/新着順/人気順)に対応させ、候補プール内のタグ使用状況(`availableTags`)も返すようにして、おすすめ記事セクションに絞り込みフォームを追加した。
  - 発見性・SEO: `Layout.astro`にOGP(`og:title`/`og:description`/`og:image`等)とtwitter:cardのメタタグを追加し(`public/og-image.svg`を新規作成)、`public/robots.txt`と`src/pages/sitemap.xml.ts`(静的ページ+上位タグの`/tags/:name`を列挙)、`src/pages/rss.xml.ts`(新着30件のRSS 2.0)を追加した。タグ別ページ`src/pages/tags/[name].astro`を新規実装し、`CatalogPage`を使って表示しつつ、内部の並び替え・ページ送り・別タグへの遷移は`/items`側の導線に合流させる設計にした。
  - 共有: `ItemCard.astro`にX(Twitter)への共有ボタン(汎用共有アイコン、intent URL)を追加した。
  - 開発者コンタクト: `Layout.astro`にフッターを追加し、`https://x.com/ml7ddw0`へのリンクを表示した。
  - 検証: `npm run build`が成功することを確認。`astro dev --background`起動後、`curl`/`node -e fetch`で以下を確認した: `/`・`/items`・`/items?sort=popular`・`/mypage`・`/rss.xml`・`/sitemap.xml`・`/robots.txt`・`/og-image.svg`・`/tags/:name`がいずれも200を返すこと、`sitemap.xml`にタグ別URLが正しくエンコードされて列挙されること、`rss.xml`が新着記事のtitle/link/description/pubDateを含むこと、ホームおよび一覧ページのヘッダーで現在地アイコンに`current`クラスが付くこと、OGP/twitter:card/RSSリンクタグがheadに出力されること、フッターの開発者リンクが表示されること。人気順ソートは実際に1件をブックマークして検証し(テスト後に削除)、ブックマーク数が多いアイテムが新着順で5番目だったものが人気順で1番目に上がることを確認した。マイページの`?tag=react`でおすすめ記事が該当タグのみに絞り込まれることも確認した。
  - `docs/plan/ui_improve_plan_20260705.md`の全項目を`[x]`にした。

- `docs/`直下のMarkdownファイルをサブフォルダに整理した（`development-roadmap.md`のみ直下に残す）。`db-schema.md`/`operations.md`→`docs/reference/`、`spec-mypage-auth.md`/`spec-brave-search-blog-import.md`→`docs/spec/`に移動し、README/`scripts/release.mjs`/ロードマップ/本ファイル内の相互参照リンクをすべて新しいパスに更新した。
- `docs/spec-brave-search-blog-import.md`（移動後は`docs/spec/brave-search-blog-import.md`）の設計に従い、Brave Search API 経由の個人ブログ収集を実装した。M3で先送りにしていた項目。
  - `src/lib/brave.ts`: Brave Web Search APIクライアント（`BRAVE_API_KEY`必須、`search_lang=jp`/`country=JP`固定）。`.env`/`.env.example`に`BRAVE_API_KEY`/`BRAVE_COUNT`を追加した。
  - `src/lib/importers/keywords.ts`に`EXCLUDED_BLOG_DOMAINS`（Qiita/Zenn/note/GitHub/YouTube/X）を追加し、検索クエリの`-site:`と結果フィルタの両方で除外する。
  - `src/lib/importers/common.ts`に`findItemVersionByExternalUrl`を追加し、既存`items.version`との比較による差分検知（本文ハッシュが同じなら再レビューを省略）に使えるようにした。
  - `src/lib/importers/blog.ts`: 発見（Brave Search、`POKEMON_KEYWORDS`ごとに1クエリ）→ 除外ドメインフィルタ（defense in depth）→ HTML fetch（`AbortController`で10秒タイムアウト、`Content-Type`がhtmlでなければskip）→ Cloudflareの`HTMLRewriter`で`article`→`main`→`body`(nav/header/footer/aside/script/style除外)の順にフォールバックしながら本文・タイトル・OGP・canonical URL・著者・公開/更新日時を抽出 → 本文200文字未満はskip → SHA-256先頭16桁を`version`として既存値と比較し不変ならAIレビューを省略 → `reviewImportArticle`→`upsertItemByExternalUrl`という一連の流れを実装した。他インポーターと異なりsourceはサービス単位ではなくドメイン単位でupsertする。
  - `src/pages/api/import/blog.ts`（Qiita等と同型のGET/POST）、`src/worker.ts`の`scheduled`に`BLOG_CRON`（`30 19 * * *`、JST翌04:30）分岐を追加、`wrangler.jsonc`の`triggers.crons`に4本目を追加、`scripts/collect/collect-blog.mjs`と`package.json`の`collect:blog`を追加した。
  - 検証: `npm run build`成功。`astro dev --background`起動中のローカルサーバーへBasic認証付きで`POST /api/import/blog`を実行し、実際のBrave Search APIを呼び出して検証した。`-site:`指定にもかかわらずBrave側が除外を完全には遵守せず上位がqiita.comばかりだったが、結果フィルタ側のdefense in depthが正しくQiitaを除外することを確認した。除外後に残った3件のブログ記事に対してfetch→HTMLRewriter抽出→OpenAIレビューまで実行され、2件は抽出した本文の具体的な内容（Zenn/Qiitaの運用の話、プラットフォーム比較記事であること等）に基づいてAIが的確にaccepted=falseと判定した（1件はAI応答のパースエラーで`skipped`として吸収され、バッチ全体は継続した）。`docs/development-roadmap.md`のM3該当項目を`[x]`にした。
- ユーザーからBrave Search APIの無料枠が月1000件（≒1日30件）である旨の共有を受け、ブログ収集を1日1回のcronのまま無料枠を有効活用できるようページ送り（`pages`）に対応させた。
  - `src/lib/importers/blog.ts`の`BlogSyncOptions`/`resolveBlogSyncOptions`に`pages`を追加し、`discoverCandidates`をキーワードごとに`pages`回オフセットをずらしてBrave検索するループに変更した（1ページの結果が`count`未満なら最終ページとみなして早期終了し、無駄なリクエストを避ける）。既定値は`POKEMON_KEYWORDS`(2語)×`pages`(既定15)=1日30件の無料枠に収まるよう`DEFAULT_PAGES=15`とし、`.env`/`.env.example`/`.dev.vars`に`BLOG_PAGES=15`を追加した（env上書き可）。
  - `BlogSyncResult`に`pages`/`requestsUsed`（実際に消費したBrave APIリクエスト数）を追加し、`src/worker.ts`の`[cron:blog]`ログにも出力した。`src/pages/api/import/blog.ts`のGET/POSTと`scripts/collect/collect-blog.mjs`（`BLOG_PAGES`環境変数）も追随させた。
  - 検証: `npm run build`成功。`astro dev`実機で`GET /api/import/blog`が`pages: 15`を返すことを確認した（無料枠温存のため、今回は30件消費するPOSTでの実行検証は見送った）。

- ユーザーから「HOMEと検索ページの間にトレンドページを作りたい」と依頼を受けた。着手前に「トレンド算出をGPTに任せるか」を確認され、`tag-explain.ts`以外のランキング系（人気順ソート・おすすめ機能）が全てGPT不使用の決定的集計であることを踏まえ、トレンドも同方針（GPT不使用）とすることで合意し、`docs/spec/trend-page.md`として仕様化した。
  - 算出ロジック: `catalog.ts`に`fetchTrendingItems`/`fetchTrendingTags`を追加。`published_at ?? created_at`が直近14日(`TREND_WINDOW_DAYS`)以内のアイテムを母集団にし、話題の記事は`bookmarks`件数降順(タイブレークは公開日降順)で上位12件、急上昇タグは母集団内の`item_tags`出現回数順で上位10件。直近14日以内にアイテムが無いコールドスタート時は期間条件を外して全体から算出するフォールバックを入れた。タグ集計は既存の`tagUsageFromItems`（おすすめ機能で使用）をそのまま再利用した。
  - 画面: `src/pages/trends.astro`を新規作成（`CatalogPage`は使わずフィルタ・ページング無しの固定表示）。急上昇タグは`TagCloud.astro`を流用しリンク先を`/items?tag=`に、話題の記事は`ItemCard.astro`をグリッド表示し、末尾に`/items?sort=popular`への「もっと見る」リンクを置いた。
  - ナビゲーション: `Layout.astro`のHOMEアイコンと検索(虫眼鏡)アイコンの間に、トレンド線アイコンの新規リンク(`/trends`)を追加した。
  - 検証: `npm run build`成功。`astro dev`起動中のローカルサーバー（ローカルSupabaseスタック、実データ72件）へ`curl`で`/trends`が200を返すこと、急上昇タグのチップと話題の記事カード（ブックマークトグル・タグホバー解説・共有ボタンなど`ItemCard`の既存機能込み）が実データで正しく描画されること、ナビゲーションの`/trends`アイコンに`current`クラスが付くことを確認した。ブックマークが0件のため実際の並び替え（ブックマーク数順）自体は未検証（既存の人気順ソートと同じロジックのため、リグレッションのリスクは低いと判断）。
  - `@astrojs/check`が未インストールで`astro check`が依存関係の追加インストールを要求したため、型チェックは`npm run build`成功の確認に留めた（依存関係の追加はユーザー確認なしに行わない方針のため）。
- 上記トレンドページについて、ユーザーから「やっぱり廃止し、HOMEに新着順/話題順のプルダウンを置くだけにする」と方針転換の指示を受けた。トレンドページ関連を全て削除し、HOMEにソート切替を追加した。
  - `src/pages/trends.astro`と`docs/spec/trend-page.md`を削除し、`src/lib/catalog.ts`の`fetchTrendingItems`/`fetchTrendingTags`/`fetchTrendWindowItems`/`fetchTrendCandidates`および`TREND_*`定数を削除、`Layout.astro`のナビからトレンドアイコンのリンクを削除した。
  - `src/pages/index.astro`（HOME）を、単純な`fetchCatalogItems`から検索ページと同じ`fetchCatalogItemsPage`（`sort: 'new' | 'popular'`）に切り替え、`sort`クエリパラメータで新着順/話題順を選べるプルダウンを追加した（マークアップ・auto-submitスクリプトは`CatalogPage.astro`の`sort-select`と同じパターンを踏襲）。話題順は既存の人気順ロジック（直近300件の候補プールをブックマーク数でJS側ソート）をそのまま再利用しており、新規のトレンド判定ロジックは持たない。
  - 検証: `npm run build`成功。`astro dev --background`起動後、`curl`で`/`・`/?sort=popular`が200、`/trends`が404になること、HOMEに`新着順`/`話題順`の`<select>`が描画されること、ナビに`/trends`へのリンクが存在しないことを確認した。

- タグの品質改善として、「ポケモン」タグの削除と、タグ表示の大文字小文字統一を行った。
  - このハブの記事は全てポケモン関連が前提のため、「ポケモン」タグは全記事に付いて情報価値が無いと判断し、`migrations/009_remove_pokemon_tag.sql`（`unaccent()`で一致するもの）と`migrations/010_remove_pokemon_tag_nfc.sql`（009では捕捉できなかった濁点分解済み表記を`normalize(..., NFC)`で一致させて削除）の2段階で削除した。あわせて`src/lib/importers/article-ai.ts`のシステムプロンプトに「ポケモン」「pokemon」など主題そのものを指すだけのタグを付けないよう明記し、再発を防いだ。
  - `normalizeTagName`によりDB上のタグ名は小文字統一済み（`ai`/`github actions`等）だが、画面にそのまま出すと読みにくいため、表示専用の変換を`src/lib/tag-display.ts`（`formatTagLabel`）として新規実装した。既知語（AI/API/LLM/GitHub/TypeScript等）は固定の公式表記に変換し、それ以外のASCII語は先頭大文字化、非ASCII（日本語）はそのまま表示する。保存値は変更せず表示時のみ変換する。`ItemCard.astro`/`TagCloud.astro`/`mypage.astro`（おすすめ絞り込みの選択肢）/`tags/[name].astro`（タグ別ページの見出し）の全タグ表示箇所に配線した。
  - 検証: ローカルSupabaseスタックに対し`lower(name)`でグルーピングして大文字小文字の重複が0件になったこと、「ポケモン」タグが0件になったことをクエリで確認した。`npm run build`が成功することも確認した。
  - 本セッション終了時点で、上記の変更（migrations 009/010・`tag-display.ts`・`article-ai.ts`のプロンプト修正・各画面への配線）は未コミットのままローカルの作業ツリーとローカルSupabaseスタックにのみ反映されている。本番Supabaseへの適用（`npm run release`）と`main`へのコミット・pushは別途必要。

## 参照
- [development-roadmap.md](../development-roadmap.md)
- [spec-mypage-auth.md](../spec/mypage-auth.md)
- [spec-brave-search-blog-import.md](../spec/brave-search-blog-import.md)
- [2026-07-04.md](2026-07-04.md)
- [docs/plan/ui_improve_plan_20260705.md](../plan/ui_improve_plan_20260705.md)
