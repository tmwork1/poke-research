# 進捗メモ 2026-07-09

## 新着記事のX投稿下書きをDiscordに通知（`feat/new-items-x-draft-digest`）

cronで新着記事（AIレビューで採用され新規insertされた記事）が見つかったとき、Xに投稿しやすい形式の下書き文をまとめてDiscordへ送る機能を追加。

- 対象は収集系cronジョブ全て（Qiita/Zenn/note/Blog(Brave Search)/RSSフィード追従/はてなブックマーク）。link-check・週次DBレビューは対象外（新着記事を扱わないため）。
- 下書きは新規のOpenAI呼び出しを追加せず、AIレビュー結果とは独立に `サイトURL＋各記事のタイトル/URL` だけで組み立てる（後述の140字制限対応の経緯を参照）。
- 送信先は既存の `ALERT_WEBHOOK_URL`（運用アラート・週次レポートと同じDiscordチャンネル）をそのまま使用。新しい環境変数は追加していない。
- 複数件見つかった場合は1件ずつのメッセージではなく、まとめて1つのDiscordメッセージに列挙する。

### 実装
- `src/lib/notify.ts`: `sendNewItemsDigest()` を追加。`action==='inserted'` の記事から下書きを組み立て、Discordへ1メッセージで送る。
- `src/worker.ts`: 各cronハンドラ（Qiita/Zenn/note/Blog/Feed/Hatena）の成功ログ直後に、`action==='inserted'` の記事があれば `sendNewItemsDigest` を呼ぶ `notifyNewItems()` ヘルパーを追加。

初版はAIレビュー生成の要約80字＋タグ由来ハッシュタグを記事ごとに付けていたが、Xの140字制限では
新着が2件以上のとき収まらないとユーザー指摘があり修正。要約・ハッシュタグは廃止し、下書きは
`topic.site.url`（PokeResearchのサイトURL）＋各記事のタイトル/URLのみのシンプルな構成にした
（`process-import-item.ts` へ一度追加したsummary/tagsの受け渡しも、不要になったため差し戻した）。

### バグ修正: `notify.ts` のDiscord判定が `discordapp.com` 形式のWebhook URLに非対応だった
ユーザーの `.env` へ一時的に本番 `ALERT_WEBHOOK_URL` を追加してもらい、`sendNewItemsDigest` を
ダミーデータで実際に呼び出したところ、Discordが400を返した。原因は `postToWebhook` の
`isDiscord` 判定が `discord.com/api/webhooks` の部分一致のみで、旧ドメイン
`discordapp.com/api/webhooks`（今回のWebhook URLの形式）にマッチしなかったこと。Slack形式
`{text}` ペイロードがDiscordに送られて拒否されていた。`/discord(app)?\.com\/api\/webhooks/` に
広げて修正（`sendOperationalAlert`/`sendMaintenanceReport` も同じ関数を使うため、このバグは
以前から運用アラート・週次レポート全体に影響していた可能性がある）。

### メッセージ形式をユーザー指定に合わせて調整
実送信の見た目を見たユーザーから指定があり、絵文字プレフィックス（🆕）とジョブ名（「Qiitaで」等）
を外し、`[poke-research] 新着 N 件` のヘッダー＋`サイトURL（末尾スラッシュ付き）＋記事タイトル/URL`
だけのシンプルな形式に変更。`sendNewItemsDigest` からジョブ名の引数を削除し、`worker.ts` 側の
呼び出し（`notifyNewItems`）も追随。

### フォーマット再調整：記事ごとに掲載元（ソース）名を付与
ユーザー指定で最終形式を `【poke-research】新着 N 件` ＋ `サイトURL` ＋ 各記事 `タイトル - ソース名`／
`URL` に変更。Qiita/Zenn/noteは掲載元が固定だが、Blog(Brave Search)/RSSフィード/はてなブックマーク
は記事ごとに実際の個人ブログ等が異なるため、ジョブ名を決め打ちにできない。新規に
`fetchItemSourceNames()`（`src/lib/importers/common.ts`）を追加し、`items.source_id` 経由で
`sources.name` を都度引くようにした。JOINの正規化は `catalog-normalize.ts` の既存
`normalizeSource`（Supabaseの結合結果が単一オブジェクト/配列どちらでも返りうる問題への対処）を
そのまま再利用している。`worker.ts` の `notifyNewItems()` が新着itemのidから逆引きして
`sendNewItemsDigest` に渡す。

### 検証
- `npm test`（96件）・`npm run build` green。
- `sendNewItemsDigest` を実際の本番 `ALERT_WEBHOOK_URL`・ダミー記事2件で呼び出し、Discordへの
  実送信を確認（初回は400、`discordapp.com`対応後に成功。以降の形式変更のたびに再送信して確認）。
- `fetchItemSourceNames` が使うJOINクエリ（`items` → `source:sources(...)`）は、ユーザーが
  Dockerでローカル Supabase を起動・接続情報を共有してくれたため、`@supabase/supabase-js` で
  実際の本番同等ローカルDB（items 280件超）に対して直接実行し、`source` が単一オブジェクトで
  返り `name`（例: "note"、"その他"）を正しく取得できることを確認した。

## 「論文」タブの新設・検索タブの改名（`feat/paper-collection`、UI担当）

別セッションで並行実装中のarXivインポーター（`items.kind='paper'`）を見越し、UI側（ナビ・一覧ページ）
を先行実装。DBには`kind='paper'`のデータがまだ無いが、フィルタ配線自体は先に済ませておく方針。

- `src/lib/catalog.ts`: `fetchCatalogSources` が受け取れるフィルタに `kind` を追加（`ItemFilters.kind`
  自体は既存・未使用だった）。`kind` 絞り込みがある場合は、ソースのチップ件数も
  `source_item_counts` RPC（全件集計）ではなく実件数カウント（`countSourceMatches`）を使うよう
  `hasNarrowingFilters` の判定に `kind` を追加（これをやらないと論文タブのソースチップ件数が
  技術記事分も含めた数字になってしまう）。
- `src/pages/items/index.astro`: `fetchCatalogItemsPage`/`fetchCatalogSources` に `kind: 'article'`
  を渡し、論文を除外。表示名は「検索」→「技術記事」に変更（`CatalogPage` の `eyebrow` も
  `"Search items"` → `"Articles"`、他の一覧ページのeyebrow命名パターン（`Recent items`/`Tags`等の
  英語短句）に合わせた）。
- `src/pages/papers/index.astro`（新規）: `items/index.astro` とほぼ同一の作りで `kind: 'paper'` を渡す
  バリエーション。`basePath="/papers"`、`eyebrow="Papers"`。タグページ（`/tags/{name}`）は記事・論文
  両方のkindを横断する既存の仕組みのため、`items/index.astro` にある単一タグ時の
  `/tags/{name}` へのcanonical正規化ロジックはここでは採用せず、クエリ付きURLは常に
  `/papers`（クエリなし）へcanonical・`noindex`扱いにする単純な実装にした。
- `src/layouts/Layout.astro`:
  - nav（`.tab-nav`）に「論文」タブ（`/papers`、本のようなストロークアイコン）をホームと技術記事の
    間に追加。既存の「検索」タブは表示テキストを「技術記事」に変更。
  - **副次的なモバイルレイアウト崩れを発見・修正**: タブが3→4個に増えたことで、`max-width: 720px`
    のメディアクエリ内で `.topbar { flex-direction: column; align-items: flex-start }` により
    `.tab-nav` が「中身にフィットする幅」になり、`flex-wrap: wrap` が機能せず右端の「マイページ」
    タブが画面外へはみ出す（横スクロールが発生する）不具合が出た。`.tab-nav { width: 100% }` を
    このメディアクエリ内に追加し、`flex-wrap: wrap` が正しく機能するようにした。
- `src/pages/sitemap.xml.ts`: `/items` に倣い `/papers` を静的URLとして追加。

### 検証
- `npm run build` green。
- `astro dev --background` で起動し、Chrome headlessスクリーンショットで確認：
  - デスクトップ幅（1400px）: `/items`（技術記事・全287件・ARTICLES eyebrow）、
    `/papers`（論文・全0件・PAPERS eyebrow、「一致するアイテムがありません」の空状態）とも
    レイアウト崩れなし。navで現在地のタブ（技術記事/論文）が緑色でハイライトされることを確認。
  - モバイル幅（390px）: 当初 `chrome --headless --screenshot --window-size=390,...` で
    「マイページ」タブが見切れる問題を発見したが、調査の結果これは一度目の`--window-size`が
    ワンショットscreenshotモードで正確に反映されていなかったこと（実際のviewport幅が390より
    広い ~480px前後になっていた）に起因すると判明。Chrome DevTools Protocol
    （`Emulation.setDeviceMetricsOverride` で厳密に390px幅を強制）で再検証したところ、
    上記CSS修正により「マイページ」タブが正しく2行目に折り返されることを確認した
    （`/`・`/items`・`/papers` いずれも確認）。
- コミット・push・PR作成はユーザー指示によりまだ行っていない（ファイル変更のみ）。

## arXiv論文インポーターの新規実装（`feat/paper-collection`、収集パイプライン担当）

上記UI実装と並行して、実際の収集パイプラインを実装。

- `src/lib/importers/arxiv-feed.ts`（新規）: arXiv APIのAtom XML応答を正規表現で解析する純粋関数
  `parseArxivFeed`。`hatena-feed.ts` の `decodeXmlEntities` を再利用し、`id`/`title`/`summary`/
  `authors`（複数）/`published`/`updated`/`categories`/`primary_category` を抽出する。arXivが
  エラー時に返す特殊entry（`id` に `/abs/` を含まない）を除外するガードを追加。
- `src/lib/importers/arxiv.ts`（新規）: `qiita.ts`（API直取得・HTML本文取得不要）を踏襲した
  `syncArxivCollection`/`resolveArxivSyncOptions`。検索クエリは `topic.config.mjs` の
  `searchKeywords` から英数字のみのもの（`pokemon`/`pokeapi`）を `all:` で束ねた既定値。
  `items.kind='paper'` で保存し、`body`/AIレビュー入力にはHTML本文の代わりにアブストラクト全文を
  使う。論文改訂（`vN`）で別記事扱いにならないよう、`external_url` はabs URLからバージョン番号を
  除去して正規化（`canonicalizeAbsUrl`）。
- `src/config/ai-review-prompt.mjs`: `buildSystemPrompt(topic, kind='article')` に第2引数を追加。
  `kind==='paper'` のときは要約を「200字程度」にし、採否基準(2)を「論文全体を通じてポケモンの
  データ・環境・ゲーム内容を主要な研究対象として扱っているか（他ゲームと並ぶ一例としての言及のみは
  対象外）」に差し替え。既存呼び出し元（qiita/zenn/note/blog/hatena、
  `scripts/db/retag-existing-items.mjs`）は引数を渡さず、既定値 `'article'` のまま非破壊。
- `src/lib/importers/article-ai.ts`: `ImportArticleReviewInput.kind?` を追加し `reviewImportArticle`
  から `buildSystemPrompt` へ配線。
- `src/pages/api/import/arxiv.ts`（新規）: 既存importerと同じGET/POSTパターンで手動起動用に追加。
- `tests/arxiv-feed.test.ts`（新規、7件）。

### 今回のスコープ外・要フォローアップ
- **cron（`wrangler.jsonc`）への組み込みは未実施**。Cloudflareアカウント全体のcron trigger数上限
  5件に対し、既存3エントリで日次収集ブロック（`*/30 21-23 * * *` の6スロット全て）・週次DBレビュー・
  フィードポーリングが埋まっているため（`cloudflare-cron-trigger-limit` の制約）。当面は
  `POST /api/import/arxiv` の手動起動のみ。定期実行するにはスロット再配分の判断が別途必要。
- **タグクラウド（`fetchTopTags`）は`kind`で絞り込まれない**（DB側RPC `top_tags` がkind非対応の
  全件集計のため）。論文タブ・技術記事タブとも同じ全体タグクラウードが出る。論文データが増えてから
  体感を見て、RPC改修が要るか判断する。
- arXivの検索クエリ（`all:pokemon OR all:pokeapi`）はタイトル・アブストラクト等全体を対象にした
  広い検索のため、「他ゲームの一例として名前が挙がるだけ」の無関係論文がAIレビュー前の候補として
  多く混ざる見込み（想定通り、AIレビューを安全網として運用）。

### 検証
- `npm test`（103件、うちarxiv-feed向け7件新規）・`npm run build` green。
- コミット・push・PR作成・cron統合はユーザー指示によりまだ行っていない（ファイル変更のみ）。

## タグクラウードのkind対応・cronへのarXiv組み込み（`feat/paper-collection`、上記2件のフォローアップ）

上記「今回のスコープ外・要フォローアップ」の2件（タグクラウードのkind非対応・cron未組み込み）を、
ユーザーとの壁打ちで方針を確定のうえ実装。

### タグクラウードのkind対応
- `migrations/023_top_tags_kind_filter.sql`（新規）: `top_tags` RPC（migrations/012）に
  `kind_filter text DEFAULT NULL` を追加し、`item_tags` → `items` へJOINして
  `kind_filter IS NULL OR i.kind = kind_filter` で絞り込む。引数を1個→2個に増やす変更は
  `CREATE OR REPLACE` だけでは別シグネチャの関数として並存し、デフォルト引数を持つ新シグネチャが
  既存の1引数呼び出しと曖昧になって `function is not unique` エラーになるため、まず
  `DROP FUNCTION IF EXISTS top_tags(integer)` してから新シグネチャを作成する形にした。
  `GRANT EXECUTE` も新シグネチャ（`top_tags(integer, text)`）に対して再実行。可視条件
  （`link_status`/`ai_accepted`）は既存の `top_tags` 自体が課していないため、今回も揃えて追加は
  していない（挙動を変えすぎない方針）。
- `src/lib/catalog.ts`: `fetchTopTags(limit = 20, kind?: string)` に第2引数を追加し、
  `rpc('top_tags', { tag_limit, kind_filter: kind ?? null })` を渡す。
- `src/pages/items/index.astro` は `fetchTopTags(20, 'article')`、`src/pages/papers/index.astro` は
  `fetchTopTags(20, 'paper')` に変更。HOME（`src/pages/index.astro`）・`src/pages/tags/index.astro`・
  `src/pages/tags/[name].astro`・`src/pages/sitemap.xml.ts` は企画上、記事・論文を横断した全体
  タグクラウードのままでよいため変更していない。
- 検証: CIと同じ使い捨てPostgres（Docker `postgres:16`、`auth.users` スタブ込み）を起動し、
  `001`〜`023` を `npm run migrate` で空DBに順次適用して green を確認。さらに `items`/`tags`/
  `item_tags` にダミーデータ（article 1件・paper 1件、共通タグ1つ＋各kind固有タグ1つずつ）を投入し、
  `top_tags(20)`（kind指定なし＝3件）・`top_tags(20, 'article')`（2件）・`top_tags(20, 'paper')`
  （2件）を直接呼び出して期待どおりの絞り込みになることを確認。`\df top_tags` で旧シグネチャ
  `top_tags(integer)` が残っていない（曖昧化しない）ことも確認した。コンテナは検証後に削除済み。
  本番Supabaseへの適用はまだ行っていない（別途ユーザー確認のうえ実施）。

### cronへのarXiv組み込み・noteの自動実行停止
- `wrangler.jsonc`: 日次収集ブロックの `"*/30 21-23 * * *"`（30分刻み6スロット）を単一発火の
  `"0 21 * * *"`（21:00 UTC = JST 06:00）に変更。arXiv収集を追加すると7ジョブになり、既存の
  30分刻みでは7個目のスロットを確保できないため、ユーザーの判断で「1回の発火にまとめてWorker内で
  順次実行する」方針を採用した。`WEEKLY_REVIEW_CRON`・`FEED_POLL_CRON` の2エントリは変更なし
  （アカウント全体で3エントリのまま、上限5件に収まる）。
- `src/worker.ts`: `controller.scheduledTime` のhh:mmで個別ジョブへ振り分けていた
  `DAILY_SLOT_HANDLERS` を廃止し、`DAILY_CRON`（`'0 21 * * *'`）1回の発火で `DAILY_JOBS`
  （関数の配列）を `for...of` で順にawaitする `runDailyJobsSequentially()` に置き換えた。
  各ジョブハンドラ（`runScheduledQiitaImport`等）自体の中身（try/catch・成功ログ・
  `notifyNewItems`・失敗時の`sendOperationalAlert`）は変更していない。新規に
  `runScheduledArxivImport()` を他ジョブと同じパターンで追加し、
  `runAndRecord('arxiv', 'cron', () => syncArxivCollection(resolveArxivSyncOptions(env)))` を呼ぶ。
  実行順序は `qiita → zenn → blog → link-check → hatena → arxiv`。
- **note を日次cronの自動実行対象から除外**: 実装中にユーザーから追加指示があり、note非公式APIが
  403 Access denied を返すようになったこと（運用アラートで検知）を理由に、cronからの自動実行のみ
  停止することになった。`src/worker.ts` の `runScheduledNoteImport` 関数・`DAILY_JOBS` からの参照・
  `note.ts`/`resolveNoteSyncOptions`のimportを削除し、代わりに削除経緯を説明するコメントを残した。
  `src/lib/importers/note.ts` 本体と手動起動用の `POST /api/import/note`
  （`src/pages/api/import/note.ts`）はどちらも変更していない（手動起動は今後も可能）。
  `wrangler.jsonc` のコメントにも除外理由を追記した。

### 実行時間についての所見（CPU時間 vs 実行時間）
6ジョブ（元々30分間隔で6回に分散していたもの）を1回のCron Trigger発火に集約したことで、
Cloudflare Workersの制限に抵触しないか確認した。
- **CPU時間**: 各ジョブの処理はOpenAI API呼び出し・外部サイトへのfetch・Supabaseへのクエリなど
  I/O待ちが大半で、実際のCPU使用時間（Workers課金・制限の対象）はJSON/XMLパースや文字列処理程度に
  留まる見込み。6ジョブを1回にまとめても、待機時間はCPU時間に計上されないため、Workersプランの
  CPU時間制限（既定は数十秒〜、`wrangler.jsonc`に`limits.cpu_ms`の指定は無く現状デフォルトのまま）
  に抵触する可能性は低いと判断した。
- **実行時間（wall-clock）**: Cloudflare の Cron Trigger は `scheduled()` ハンドラ・
  `ctx.waitUntil()` に渡した処理を合わせて一定時間内（目安として最大15分程度）に完了する必要が
  あるとされ、6ジョブの合計時間がこの範囲に収まるかは実測していない。以前は30分間隔で分散して
  いたため個々のジョブの所要時間が長くても影響が出にくかったが、集約後は「6ジョブの合計」が
  1回の発火の時間予算に収まる必要がある。深刻な懸念とまでは判断せず実装を進めたが、
  本番適用・デプロイ後は初回数回の実行時間をCloudflareダッシュボード/ログで確認し、予算に近い
  場合はBrave Search（ページ数が多いと時間がかかりうる）等の重いジョブを見直すか、
  アカウント全体で cron trigger にまだ2枠の余裕がある（現在3/5使用）ため、必要なら再度スロットを
  分割する対応を検討したい。

### 検証
- `npm run build`・`npm test`（103件、変更なし）green。
- マイグレーション（`023`）は使い捨てPostgres（Docker）で `001`〜`023` の順次適用・`top_tags`の
  kind絞り込み動作を確認（上記）。**本番Supabaseへの適用は今回行っていない**（ユーザー確認後に
  別途実施）。
- コミット・push・PR作成は行っていない（ファイル変更のみ）。

## 日次収集Discord通知の0件対応（PR #36）と、cron動作確認で発覚したsubrequest上限問題

### 背景・変更内容（PR #36）
これまで日次収集ジョブ（Qiita/Zenn/Blog/はてな/arXiv/フィード、計6ソース）は各ジョブが個別に
`sendNewItemsDigest` でDiscordへ通知しており、新着0件のソースは何も送信しない仕様だった。
そのため「cronが正常に実行されたかどうか」を通知だけでは確認できないという課題があり、
ユーザー指示で以下に変更した。

- `src/lib/notify.ts`: `sendNewItemsDigest`（ソース別即時通知、0件は送信しない）を廃止し、
  `sendDailyDigest(env, items, breakdown)` に置き換え。`breakdown`（ソース名と件数の配列）を
  ヘッダーに含め、`合計0件でも必ず送信する`（cronの生存確認シグナルとして機能させる）。
- `src/worker.ts`: `runScheduledXxxImport` 系の各関数を `Promise<void>` から
  `Promise<ImportItemOutcome[]>` に変更し、ジョブ内で個別に通知していたのをやめて結果を返すだけに
  した。`runDailyJobsSequentially()` が全ジョブの結果を集約し、ループ完了後に
  `notifyDailyDigest()` を1回だけ呼ぶ構成に変更。リンク切れ検出は「新着」の概念がないため集約対象
  （`DAILY_COLLECTION_JOBS`）に含めず、元の実行順（ブログの後・はてなの前）を保ったまま個別に呼ぶ。
- ローカルの`ALERT_WEBHOOK_URL`（本番とは別のテスト用チャンネル）に実際に0件Digestを送信し、
  期待通りのメッセージ（`【poke-research】本日の収集結果: 合計0件（フィード 0件・Qiita 0件・...）`）
  が届くことを確認したうえでmainへマージ。

### cron動作確認と、そこで発覚した重大な問題
PR #36が実際のCloudflare Cron Triggerで正しく動くかを確認するため、日次cronの発火時刻を
一時的に `0 0 * * *`（JST 09:00）から `15 1 * * *`（JST 10:15）に変更して本番で観察した
（PR #37）。結果、以下が判明した。

- 10:15 JSTにcronは正常に発火し、1番目のジョブ（フィード）は成功して`import_runs`に記録された。
- しかし2番目のQiitaジョブ以降、`import_runs`に何も記録されず、Discordへの日次サマリ通知
  （PR #36の新機能）も一切届かなかった。cron発火から10分以上待っても状況は変わらなかった。
- 同じ`POST /api/import/qiita`を本番へ手動で直接叩いて再現を確認。`curl --max-time 480`
  （8分の余裕を持たせたクライアント）で待ったところ、**約160秒後にHTTP 201で応答した**。
  ハング（無限に応答が返らない）ではなく、大幅な遅延と大量のエラーが発生していたことが判明。
- レスポンス本文に明確な原因が含まれていた:
  `"reason": "Too many subrequests by single Worker invocation. To configure this limit, refer to https://developers.cloudflare.com/workers/wrangler/configuration/#limits"`
  **Cloudflare Workersの「1回のWorker呼び出しあたりのsubrequest（外部fetch呼び出し）数上限」を
  超過していた。** Qiitaの候補記事20件を処理する過程で、1件ごとにOpenAIレビュー呼び出し＋
  Supabaseへの複数回の読み書きがsubrequestとして積み上がり、上限（無料/標準プランの目安は
  50件/呼び出し）へ到達していた。
- **cron発火時に何も記録されなかった理由**: `import_runs`への記録処理自体もSupabase呼び出し
  （＝subrequest）であるため、上限到達後はその記録すら書き込めない。一方、手動APIでの単体呼び出し
  は新しい呼び出しとしてsubrequest予算がリセットされるため、Qiita単体では（大量のエラーを
  出しつつも）最終的に完了できた。
- **より深刻な点**: cron側は日次収集7ジョブ（フィード/Qiita/Zenn/Blog/リンク切れ検出/はてな/
  arXiv、noteは403エラーのため対象外）を「Cloudflareアカウント全体のcron trigger数上限5件」を
  回避する目的で**1回のWorker呼び出しに集約する設計**（本ドキュメント上部、arXiv追加時の変更を
  参照）になっている。subrequestの上限は呼び出し単位で累積するため、Qiitaの時点で上限に達すると
  Zenn/Blog/はてな/arXivと最後のDiscord通知まで**巻き添えで失敗する**。実行時間についての所見
  （本ドキュメント上部）ではCPU時間・wall-clock時間の観点でのみリスクを検討しており、
  subrequest数の観点は当時見落としていた。

### 今回の対応（暫定）とフォローアップ
時間の制約上、今回はcronの発火時刻を`0 0 * * *`（JST 09:00）に復旧するのみ行った（PR #39）。
設計上の根本対応（ジョブ分割、Cloudflare Queues経由での独立呼び出し化、
`wrangler.jsonc`の`limits`設定見直し等の比較検討）はユーザー判断で**別セッションに持ち越し**。
`wrangler.jsonc`・`src/worker.ts`のコメントに問題の詳細を記録済み。

**未解決のため次回セッションで検討が必要な事項:**
- 日次収集ジョブを1回のWorker呼び出しに集約する現行設計は、Cloudflareのcron trigger数上限
  （5件）は回避できてもsubrequest数上限と衝突する。両方の制約を満たす設計への見直しが必要。
- `src/lib/openai.ts`のfetch呼び出しにタイムアウト・AbortControllerが無いことも合わせて確認済み
  （今回のsubrequest上限とは別軸の潜在リスク。OpenAI呼び出しが応答しないケースがあれば
  無期限にハングしうる）。
- 日次収集がこの問題により、実装当初（arXiv追加によるジョブ集約、本ドキュメント上部を参照）から
  断続的に機能していなかった可能性がある。過去の`import_runs`を遡って影響範囲を確認するのが望ましい。
