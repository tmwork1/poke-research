# 進捗メモ 2026-07-06

## 今日の作業
- ユーザーから「タグ生成プロンプトを実験で改善し、必要十分な条件を決めてほしい」と依頼を受けた。ローカルSupabaseスタック（items 13件）の`item_tags`と`items.metadata.ai`（収集時のAI判定結果の生ログ）を突き合わせ、実データで現状の問題点を洗い出した。
  - 「システム開発」（13件中7件）「設計パターン」（4件）「データベース設計」（4件）が、ROMハック記事（id11-13）・TiDBハイブリッド検索記事（id6）・TypeScript型システム記事（id8）など内容の異なる複数記事に判で押したように付いていた。特にid11-13（ROMのメモリ改造）は内容的に「設計パターン」「データベース設計」と無関係で、AIが安全策として汎用語に倒れていると判断した。
  - 「ポケモンカート」というタグ（id10「ポケポケで初手カスミが使える確率」に付与）を調査した。`items.metadata.zenn.topics`（収集元Zennのトピック）は`["prolog","ポケモン","確率"]`で「カード」系の語は無く、AIが要約文中の「ポケモンカードゲーム」という表現から新規タグを生成する際に「ド」を「ト」に取り違えたものと判明した（Unicode正規化の問題ではなく、生成テキスト自体の誤字。実際に文字コードを確認し、濁点付き「ド」ではなく濁点なし「ト」であることを確認した）。既存タグ「ポケモンカード」が`existing_tags`として渡っていたにもかかわらず発生しており、既存タグ再利用ロジックのすり抜けではなく、モデル自身の生成時のカタカナ誤字（トークンレベルの確率的な誤り）であると特定した。
- 「必要十分なタグ」の判断基準を次のように定めた。
  - そのタグ単体で検索した際に、無関係な記事まで大量にヒットしてしまうような一般語（システム開発、設計パターン、プログラミング、開発、実装 等）は使わない。記事固有の技術要素（具体的な言語・フレームワーク・ライブラリ・アルゴリズム・手法名）と、記事が扱うポケモン側の具体的対象（カードゲーム、ROM改造、対戦・育成、図鑑データ等）を優先する。
  - 新規タグを作る前に必ず`existing_tags`を確認し、同義・類似・綴りが近い語があればそれを再利用する（表記ゆれ・カタカナの濁点半濁点の誤字を含む）。
  - タグ数は3〜5個（既存の`MAX_AI_TAGS`のまま）。
- OpenAI課金を避けるため、DB内の実データ（title/summary/収集元タグ）を使い、`article-ai.ts`の`getOpenAIConfig()`/`OPENAI_CHAT_COMPLETIONS_URL`を流用した使い捨てNodeスクリプトで、13件×複数のプロンプト案×複数試行をオフライン実験した（本番コードは変更せず、実験用スクリプトはスクラッチパスに作成・実行後に削除）。
  - A（現行プロンプト）: 「システム開発」等の汎用語が高頻度（7/13等）で残り、「ポケモンカート」誤字もカード関連5記事中4記事で発生。
  - B（汎用語回避の指示のみ追加）: 汎用語は大きく減った（システム開発0件）が、「ポケモンカート」誤字がカード関連6記事全てで発生し悪化した。
  - C（汎用語のブロックリスト例示+具体性優先の指示）: 汎用語はBと同程度に減り、誤字はやや改善（5記事中2記事は正しく「ポケモンカード」）。
  - D（Cに加えて「ポケモンカード」の正しい表記を明示するアンカー文を追加）: 3試行×13件でカード関連の正誤を集計したところ、誤字率がC比で改善（正答率27%→53%）した一方、ROMハック記事（id11-13）で「設計パターン」等の汎用語が逆に増える回帰が見られた（プロンプト末尾に追記した誤字対策の文が、冒頭の汎用語回避の指示を薄めた可能性）。
  - E（汎用語回避とアンカー文を1つの指示ブロックに整理統合し、優先度を(a)(b)で明示）: 3試行×13件で最も良好な結果。「システム開発」はid1（記事自体が設計論の内容で妥当な使用）を除きほぼ消失、「設計パターン」の誤用はid11で1/9試行のみに抑制、id12/13は3/3・2/3で汎用語ゼロ。カード関連の正答率は65%（11/17）とD超えで最良。ただし誤字を完全には根絶できなかった（残り35%は依然「ポケモンカート」と誤生成）。
  - 誤字が完全に0にならない点は、`gpt-5-nano`というモデル自体の生成時のカタカナ誤字傾向によるもので、プロンプト文言だけでは確率的に抑制できても根絶はできないと判断した。決定的な根絶にはコード側でのタグ類似度マッチング（レーベンシュタイン距離等による近似マージ）が必要だが、今回のタスク範囲（プロンプト実験と`existingTags`優先ロジック等の既存の仕組みの維持）を超えるため、既知の残存リスクとして記録するに留めた。
- 実験結果を踏まえ、E案を`src/lib/importers/article-ai.ts`のシステムプロンプトのタグ生成部分に反映した。JSON出力契約（accepted/summary/tags/reason/confidence）、`existingTags`優先ロジック、`normalizeAiTags`/`MAX_AI_TAGS=5`、タグの生表記を保持する`tagLabels`（別セッションで先行実装済みの仕組み）はいずれも変更していない。変更したのはタグに関する指示文のみで、(a)汎用語回避＋具体的な技術要素/ポケモン側対象を優先、(b)`existing_tags`確認の徹底＋カタカナ濁点半濁点の誤字注意（「ポケモンカード」の正しい表記を明示）の2点を明記する形にまとめた。
- Before/After比較（抜粋）:
  - id11「ポケットモンスター ダイヤモンド後期ROMでシンプルエディタを導入する」: Before（現行プロンプト、実データ）`romハック,メモリエディタ,設計パターン` → After（E案、3試行中2試行）`romハック,メモリエディタ,アルゴリズム`（内容と無関係な「設計パターン」が大部分の試行で消失）。
  - id6「TiDBのハイブリッド検索を、ポケモンのパーティ構築で試してみる」: Before `システム開発,設計パターン,データベース設計` → After `データベース設計,api,python,tidb`（記事が実際に扱うTiDB固有の技術要素に置き換わった。「データベース設計」は記事の主題そのものであり妥当なため残存）。
  - id9「【強化学習】ポケモンカードAIコンペの全貌」: Before `強化学習,ポケモンカード,マルチエージェント,aiエージェント`（正しい表記）→ After（E案、3試行中2試行）`強化学習,ポケモンカード,マルチエージェント,aiエージェント`（維持）、1試行のみ「ポケモンカート」が混入する残存誤字を確認。
- 検証: `npm run build`成功。`node --env-file=.env ./scripts/eval/eval-tags.mjs`と`./scripts/eval/eval-filter.mjs`をローカルSupabaseスタック（既存items 13件）に対して実行し、既存の保存済みタグ・採否判定に変化がないこと（今回の変更は将来の収集にのみ影響し、既存データの遡及的な再タグ付けは対象外）を確認した。
- 本変更の効果測定は、実データ13件から作成したオフラインの複製入力（title/summary/収集元タグをコピーしAPIへ再送）によるものであり、実際の収集経路（Qiita/Zenn/note経由の`reviewImportArticle`呼び出し、本文全文を使った`bodyExcerpt`）での効果は次回以降の実収集でのみ確認できる。

## 現在の全体状況
- M5「タグ精度の最適化」を継続。タグ生成プロンプトの改善は完了し実データ実験で効果を確認済みだが、実収集での再検証は未実施。「ポケモンカート」のような生成時のカタカナ誤字は今回のプロンプト改善で発生率を下げたが根絶はできておらず、根絶にはコード側の近似マッチング等の追加実装が必要（別タスクとして保留）。

- タグ生成プロンプトの変更を確認する過程で、`src/lib/importers/common.ts`の`ensureTags`が新規タグ作成時に`tags.display_name`列へ書き込む実装（別セッションで先行実装済みの、タグの元表記を保持する`tagLabels`機能）に対応するマイグレーションが存在しないバグを発見した。実際に`display_name`付きでタグをINSERTしたところ`PGRST204: Could not find the 'display_name' column`で失敗することを確認し、新規タグが1件でも生成される次回の収集が全て失敗する状態だった。ユーザー確認のうえ、`migrations/011_add_tag_display_name.sql`（`tags.display_name text`列を追加）を作成し、ローカルSupabaseスタックに適用した。再度INSERTして成功すること、`npm run build`が通ることを確認済み。**本番Supabaseへの`011`適用はまだ行っていない。**

## 残課題
- **`migrations/011_add_tag_display_name.sql`の本番適用が未実施。** デプロイ前に必ず本番Supabaseへ適用すること（`docs/reference/operations.md`のマイグレーション手順に従う）。適用しないまま次の収集が走ると、新規タグ作成のたびに`display_name`列不足で失敗する。
- タグ生成プロンプト改善（`article-ai.ts`）の効果検証は、DB内の実データを複製したオフライン再送実験によるものであり、実際の収集経路（Qiita/Zenn/note経由の`reviewImportArticle`、本文全文の`bodyExcerpt`を使用）ではまだ検証していない。次回の実収集時に、汎用語（システム開発/設計パターン等）の出現頻度と「ポケモンカード」表記の正答率を確認すること。
- カタカナの濁点・半濁点誤字（「ポケモンカート」等）はプロンプト文言による確率的抑制のみで、モデル(`gpt-5-nano`)の生成傾向自体に起因するため完全には根絶できていない（実験では残存率35%程度）。確実に防ぐには、タグ保存前にレーベンシュタイン距離等で既存タグとの近似マッチングを行うコード側の仕組みが別途必要（未着手）。
- DBに既に残っている誤字タグ「ポケモンカート」（id10の記事に付与）自体の遡及的な修正・統合は今回のスコープ外で未実施。放置すると検索・タグクラウドに誤字タグが露出し続ける。
- タグの`explanation`（AI解説）が28件中7件にしか付いていない問題への対応方針は未決定のまま保留中（バックフィルスクリプト追加／新規タグ作成時トリガー／表示箇所拡大の3案を提示済みだが、ユーザーからの選択がまだない）。

## 追記: Brave Search定期実行の本番反映（同日後続作業）
- 「Brave Searchの定期実行はちゃんと設定できているか」との確認依頼を受け、本番の状態を調査した。`wrangler.jsonc`のcronトリガー追加・`src/worker.ts`の振り分け・`src/lib/importers/blog.ts`はいずれもコード上は正しいが、(1) 本番Cloudflare Workerに`BRAVE_API_KEY`シークレットが未設定、(2) その変更自体を含む一連の未コミット差分が本番に一度もデプロイされていない、ことが判明した（直近の実コードデプロイは2026-07-05T11:32で、Brave Search機能を含まない）。
- `wrangler secret put BRAVE_API_KEY`で本番に設定（`.env`と同値）。
- 未コミットだった変更一式（Brave Search収集、タグ`display_name`/AI解説、一覧・マイページの並び替え/ページネーション、RSS/サイトマップ/robots.txt等のSEO対応、ドキュメント再編、サブエージェント定義）を機能単位で6コミットに分割してコミットした。
- 本番マイグレーション`008_add_tag_explanations.sql`・`011_add_tag_display_name.sql`が未適用だったため、本番`DATABASE_URL`（ユーザー提供、パスワードに未エスケープの`/`が含まれておりURLとして不正だったためパーセントエンコードして修正）で`scripts/db/test-db.mjs`によるCRUD疎通確認後、`npm run migrate`相当のスクリプトで適用した（`011`は前日の残課題として記録されていたもの）。
- `main`へpushしてCloudflareの自動デプロイ（GitHub連携）を実行。GitHub Actions CIの`build`ジョブは`node:module`の`registerHooks`未対応エラーで失敗したが、これは今回のpush以前（2026-07-05T11:32時点のコミット）から存在する既存の問題であり、今回の変更が原因ではないことをログ比較で確認した。Cloudflare側の自動デプロイはGitHub Actionsとは別パイプラインのため影響を受けず、push直後に新しいデプロイが作成され成功した。
- デプロイ後、本番`GET /api/items`・`GET /`・`GET /rss.xml`・`GET /sitemap.xml`・`GET /robots.txt`が200であることを確認。`GET /api/import/blog`で`hasToken: true`を確認し、`POST /api/import/blog`（`pages=1, count=5`の小規模実行）でBrave Search APIへの認証付きリクエストが正常に完了する（`requestsUsed: 2`、エラーなし）ことを確認した。今回はキーワード上位が除外ドメインばかりで`fetched: 0`だったが、これは想定内の挙動でAPI疎通自体は成功している。
- **残課題（別途）**: GitHub Actions CIの`build`ジョブが`node:module`の`registerHooks`エクスポート欠如で恒常的に失敗している（Node 22.12.0固定 vs 依存パッケージが要求するNode APIの不整合の可能性）。CI自体はCloudflareの自動デプロイをブロックしないが、CIの「緑」を運用の健全性指標として使えない状態が続いているため、別タスクとして原因調査・修正が必要。

## 追記: フッター連絡先の削除・定期実行開始時刻の変更（同日後続作業）
- ユーザー依頼に基づき、`src/layouts/Layout.astro`のページ最下部フッター（`<footer class="site-footer">`のコンタクトリンク）を削除した。サイドバーメニュー内の同種のコンタクトリンク（@ml7ddw0）は、ユーザーに確認のうえ残すことにした（「ページ最下部」の指示範囲外のため）。未使用になった`.site-footer`のCSSも削除した。
- Cloudflare Cron Triggers（`wrangler.jsonc`の`triggers.crons`）の収集開始時刻を、JST翌3:00開始（UTC 18:00始まり）からJST 6:00開始（UTC 21:00始まり）に変更した。Qiita 21:00→Zenn 21:30→note 22:00→ブログ 22:30 UTC（＝JST 6:00→6:30→7:00→7:30）の順で30分間隔は維持。`src/worker.ts`の`ZENN_CRON`/`NOTE_CRON`/`BLOG_CRON`定数も同時に更新し、`wrangler.jsonc`の`crons`配列と対応が崩れないようにした。`README.md`内の該当箇所（M系セクションの本番cron説明3箇所）も新しい時刻に更新した。
- 検証: `npm run build`成功。本番Cloudflareへのデプロイは未実施（ローカルの変更のみ）。

## 追記: サイト改善案の棚卸し（同日後続作業）
- 「このウェブサイトの改善案を可能な限り列挙して文書化して」との依頼を受け、コードベース（トップ/検索/マイページ/タグページのUI、`catalog.ts`、`worker.ts`、Layout）と既存の残課題を確認したうえで、改善案を [docs/plan/site-improvement-ideas-20260706.md](../plan/site-improvement-ideas-20260706.md) に整理した。検索・発見性 / UI・UX / コンテンツ品質・収集 / パフォーマンス / SEO・シェア / 運用・信頼性 / コード整理の7カテゴリ・計50件超を優先度・工数付きで列挙し、優先着手の推奨8件（CI修復、収集失敗通知、タグ近似マッチング、タグ・ブックマーク集計のDB側移行など）を末尾にまとめた。文書化のみで、コード変更は行っていない。
- ユーザーフィードバックを受けて同文書を更新した。(1) カードの複数カラム表示案は「1カラムが最も見やすい（Qiitaと同様）」との判断で見送り（決定として記録し再提案しない）。(2) 本番で一度収集したが DB のアイテム数が非常に少ない件を優先度高の新項目として追加（増分収集前提の既定値と AI レビュー棄却が要因の見込み。内訳調査 + pages/perPage を上げた過去分バックフィルを提案）。(3) AI要約が冗長でですます調のため、常体・2行程度への圧縮（`article-ai.ts` のプロンプト変更 + 既存要約の再生成）を優先度高で追加。推奨リストは上位10件に改訂し、バックフィル調査を1位、要約簡潔化を4位に置いた。

## 追記: 改善案上位10件の実装（同日後続作業）
[site-improvement-ideas-20260706.md](../plan/site-improvement-ideas-20260706.md) の「優先着手の推奨（上位10件）」を実装した。各項目をトピック別ブランチ（`fix/ci-node-24`、`feat/collect-backfill`、`feat/cron-failure-alert`、`fix/ai-summary-style`、`feat/tag-fuzzy-match`、`feat/db-side-aggregation`、`fix/bookmark-error-toast`、`feat/relevance-sort`、`feat/about-page`、`feat/og-png-jsonld`）で実装し、それぞれ `npm run build` を通したうえでローカル `main` へマージ済み（**push は未実施**）。

1. **本番アイテム数の原因調査**: 読み取り専用クエリで本番を確認。items 9件（Qiita 4 / Zenn 5 / note 0、blog ソース行なし）、タグ26件、誤字タグ「ポケモンカート」は存在せず。主因は増分収集前提の既定値（Qiita 1ページ×20件、note 1×10、キーワード「ポケモン」「ポケカ」のみ）+ AIレビュー棄却。対策として `keywords.ts` を6語（+ポケットモンスター/pokemon/pokeapi/ダメージ計算）へ拡充し、`npm run collect:backfill`（`scripts/collect/backfill.mjs`。pages/perPage を引き上げて全プロバイダを順に実行、AI棄却理由の内訳も出力、Basic認証対応で本番にも実行可）を追加。**本番でのバックフィル実行は OpenAI 課金を伴うため未実施・ユーザー判断待ち。**
2. **CI修復**: `ci.yml` の Node を 22.15.0 → 24 へ（Vite 8 の `registerHooks` 要求とローカル検証済み環境に整合）。push 後に緑を確認する。
3. **収集失敗通知**: `src/lib/notify.ts` を新設し `worker.ts` の各 cron catch から Webhook 通知。**有効化には Workers シークレット `ALERT_WEBHOOK_URL`（Discord/Slack Webhook URL）の設定が必要**（未設定なら従来通りログのみ）。
4. **AI要約の簡潔化**: `article-ai.ts` のプロンプトを「常体（だ・である/体言止め）・2文以内・120字程度」へ変更。既存9件はバックフィル実行時に再生成される。
5. **タグ近似マッチング**: `ensureTags` 前段で編集距離1の既存タグへマージ（日本語含む5文字以上限定、数字違いの世代・バージョンは温存）。汎用の遡及統合スクリプト `scripts/db/merge-tag.mjs <from> <to>` も追加（冪等）。
6. **集計のDB側移行**: `migrations/012_add_aggregate_functions.sql` で `top_tags` / `bookmark_counts` RPC を追加し、`fetchTopTags` / `fetchBookmarkCounts` / `fetchTopTagNames` の全行取得+JS集計を置き換え。Docker の postgres:16（CI と同構成）で全マイグレーション適用と RPC の動作を確認済み。**デプロイ前に本番 Supabase へ 012 の適用が必須**（未適用だと全ページが 500。ローカル dev サーバーで確認済みの挙動）。
7. **ブックマーク失敗トースト**: `ItemCard.astro` のトグル失敗時に「お気に入りの保存/削除に失敗しました」を表示（role=alert、3秒で消える）。
8. **関連度順ソート**: `/items` に `sort=relevance` を追加。人気順と同じ候補プール方式（300件）で、タイトル一致（重み3）> 要約一致（重み1）のトークン別スコア、同点は新着順。キーワード入力時のみ選択肢に表示。
9. **About + AI要約明示**: `/about` を新設（収集の仕組み、AI生成の明示、著作権・削除依頼窓口 @ml7ddw0）。サイドバーにリンク追加。カード要約に「AI要約」バッジを付与。
10. **OG画像PNG化 + JSON-LD**: `og:image` を PNG（1200x630、resvg で SVG から生成、SVG は生成元として残置）へ変更し width/height メタも追加。`WebSite`+`SearchAction` を全ページ、`ItemList` をトップ/検索/タグ一覧に埋め込み。

## 残課題（今回分）
- **push・デプロイが未実施**: ローカル `main` が origin より大幅に先行（今回の10項目 + 従前の7コミット）。push（= Cloudflare 自動デプロイ）の前に **本番 Supabase へ `migrations/012` を適用すること**（適用しないと `top_tags` RPC 不在で全ページ 500）。
- **バックフィルの本番実行が未実施**: `IMPORT_BASE_URL=https://poke-research.com ADMIN_USERNAME=... ADMIN_PASSWORD=... npm run collect:backfill`。OpenAI 課金と Qiita 認証なし 60req/h のレートリミットに注意。実行すれば既存アイテムの要約も新プロンプト（常体・簡潔）で再生成される。
- **`ALERT_WEBHOOK_URL` シークレットが未設定**: 失敗通知を有効にするには `wrangler secret put ALERT_WEBHOOK_URL` で Discord/Slack の Webhook URL を設定する。
- 関連度順ソートの実ページ確認は 012 適用後に行う（現状はロジック単体検証と build のみ）。

## 追記: トップページ無限スクロールのUX改善（同日後続作業、`feat/infinite-scroll-ux` ブランチ）
`src/pages/index.astro` の無限スクロール周りを改善した（ブランチのまま、`main` へは未マージ）。

- **状態保持**: 追加読み込みごとに `history.replaceState` で `?page=N`（tag/sort維持、page=1のときはpage省略）をURLへ反映するようにした。`?page=N`（N>1）を直接開いた場合（記事リンクから「戻る」で戻ってきた場合など）は、1〜Nページ分をまとめて再現する実装にはせず、「1ページ目から表示」リンク付きの軽量な案内文を出すのみに留めた（`fetchCatalogItemsPage` がpage=Nをそのページ単体として扱う既存仕様に合わせたもの）。
- **ローディングスケルトン**: 「読み込み中...」テキストを、タイトル/要約2行/タグのグレーバーをパルスアニメーションさせるスケルトンカード2枚に置き換えた。色は既存のCSS変数（`--card-bg`/`--border`/`--bg-soft`）のみを使うためライト/ダーク両対応。`prefers-reduced-motion: reduce` 時はアニメーションを止める。
- **スクリーンリーダー通知**: `aria-live="polite" role="status"` のビジュアリーヒドゥン要素（`CompactToggle.astro` と同じclipパターンで`.sr-only`化）を追加し、追加読み込み完了時に「N件追加されました」をセットする。
- **空状態文言**: 開発者向けの「scripts/db/seed-sample-article.mjs でサンプルを投入できます」という文言を廃止。タグ絞り込みで0件のときは「このタグの記事はまだありません。」+「すべての記事を見る」リンク、絞り込みなしで0件のときは「記事を準備中です。しばらくしてからご覧ください。」に変更した。
- **ページング方針の文書化**: [docs/reference/pagination-policy.md](../reference/pagination-policy.md) を新設し、トップ=無限スクロール（回遊・新着チェック向け）/ `/items`・タグページ=ページネーション（検索結果の位置共有・SEO向け）という使い分けの意図を記録した。

検証: `npm run build` は今回の指示で実行禁止のため未実施。稼働中のdevサーバーに対し `curl` でSSR出力を確認した。
- `curl -s "http://localhost:4321/" | grep -o skeleton` → `skeleton` がヒット（スケルトンカードが初期HTMLに含まれる）。
- `curl -s "http://localhost:4321/?tag=<存在しないタグ>"` → 本文に「このタグの記事はまだありません。」+「すべての記事を見る」リンクを確認。
- `curl -s "http://localhost:4321/?page=2"` → status 200、`resume-notice`（「1ページ目から表示」リンク、tag/sortを維持したURL）が本文に出現。`?page=1`（トップ）側には出現しないことを確認。
- `curl -s "http://localhost:4321/?tag=Python&page=2"` → 「1ページ目から表示」リンクが `/?tag=Python`（tag維持・page省略）になることを確認。

気づいた問題（今回のスコープ外、修正はしていない）: `?page=9999` のような総ページ数を超える値を指定すると、`fetchCatalogItemsPage` 側で例外が発生し「データの取得に失敗しました」というエラー表示 + 空状態メッセージが同時に出る（ステータスコード自体は200のまま500にはならない）。ブランチ作業前の `main` でも同一の挙動であることを `git stash` で確認済みのため、既存の別課題として記録するに留めた。

## 追記: 人気順ソートをカウンタ列によるDBソートへ本実装（同日後続作業、`feat/popular-sort-db` ブランチ）
「サイト改善案」の残課題だった人気順ソートの暫定実装（`POPULAR_SORT_CANDIDATE_POOL=300`件をJS側でソート、プール外の記事は人気順に出ない問題）を解消した。`main` から `feat/popular-sort-db` を切って作業し、**ブランチのままマージしていない**。

- `migrations/013_add_bookmarks_count.sql`（前セッションで作成済みだったものをレビューし、変更なしでそのまま採用）: `items.bookmarks_count integer NOT NULL DEFAULT 0` を追加し、`bookmarks` の INSERT/DELETE トリガー（`sync_items_bookmarks_count`）で同期。DELETE側は `GREATEST(bookmarks_count - 1, 0)` で負値を防止。既存データは移行時に集計UPDATEで初期化。`idx_items_bookmarks_count`（DESC）も追加。
- `src/lib/db-types.ts`: `Item` に `bookmarks_count?: number` を追加。
- `src/lib/catalog.ts`:
  - `ITEM_SELECT` に `bookmarks_count` を追加。
  - `queryCatalogItems` に `options.orderBy?: 'published_at' | 'bookmarks_count'` を追加し、`'bookmarks_count'` 指定時は `bookmarks_count DESC → published_at DESC → created_at DESC` でDB側ソートする分岐を追加（既存の new/oldest 用の並びは変更なし）。
  - `fetchCatalogItemsPage` の `sort === 'popular'` 分岐を、300件プール取得＋JSソートから `queryCatalogItems({ ...filters, limit: pageSize }, { withCount: true, offset, orderBy: 'bookmarks_count' })` による通常のDB側ページングに置き換え。`POPULAR_SORT_CANDIDATE_POOL` 定数は削除。全件が人気順の対象になった。
  - `fetchBookmarkedItemsFiltered` / `fetchRecommendedItems` 内の `fetchBookmarkCounts`（RPC）利用は指示通り変更していない（対象が少数のため簡略化の必要性が薄いと判断）。
- 一覧描画側（`src/pages/index.astro` / `src/pages/items/index.astro` / `src/pages/tags/[name].astro` / `src/components/CatalogPage.astro`）: ページごとに `fetchBookmarkCounts`（RPC呼び出し1本）を実行していたのをやめ、`item.bookmarks_count ?? 0` を直接 `ItemCard` の `bookmarkCount` に渡すよう変更。`CatalogPage.astro` の `bookmarkCounts` prop は不要になったため削除。`mypage.astro`（ブックマーク一覧・おすすめ）は指示通り変更していない。

### 検証
- ローカルの使い捨て Docker `postgres:16`（`poke-mig-test-2`、ポート55433）に対し、CI（`.github/workflows/ci.yml` の `migrations` ジョブ）と同じ手順（`auth.users` スタブ作成 → `npm run migrate`）で `001`〜`014`（未追跡の014含む）まで全マイグレーションが空DBから順に適用できることを確認。
- 同コンテナで `psql` により `items` へのレコード挿入→`bookmarks` へのINSERTで `bookmarks_count` が0→1に、DELETEで1→0に増減することを直接確認し、トリガーの整合性を確認した。
- `npm test`（14件）・`npm run build` はいずれも成功。
- 稼働中のdevサーバー（本番Supabaseを参照、`013`未適用）に対し `curl` でSSR出力を確認した。`GET /items?sort=popular` および `GET /items`（新着順）・`GET /items?sort=oldest` は `500`（`column items.bookmarks_count does not exist`、devサーバーのログで確認）、`GET /?sort=popular` は既存の `try/catch` により `200`（ページ内に「データの取得に失敗しました」のエラー表示が出る形。この非対称はこの変更以前からの `index.astro` の既存エラーハンドリングの挙動であり、今回新たに導入したものではない）。**これは想定どおりの失敗であり、本番へ `migrations/013` を適用すれば解消する見込み。** `GET /` 通常表示・`GET /tags/Python` は影響なく200。

### 残課題・注意点
- **本番 Supabase への `migrations/013` の適用は未実施。** 依頼者の指示によりこのタスクでは実施していない。マージ・デプロイ（`main` への push）より前に、`docs/reference/operations.md` の手順に従って本番へ適用する必要がある。適用しないまま `main` へマージ・デプロイすると、`/items`・`/tags/*` は500、トップページはエラー表示（200だが人気順・新着順とも記事0件相当の表示）になる。
- `feat/popular-sort-db` ブランチはマージしていない（指示によりブランチのまま終了）。

## 追記: 匿名アクセスへの Cache-Control 付与（同日後続作業、`feat/anonymous-cache` ブランチ）
Cloudflare エッジ/共有キャッシュを効かせて Supabase 読み負荷と TTFB を下げるため、`src/middleware.ts` に未ログインGETページ向けの `Cache-Control` 付与を実装した。`main` から `feat/anonymous-cache` を切って作業し、**ブランチのままマージしていない**。

- 対象パスは `/`・`/items`・`/tags`（`/tags/*` 含む）・`/about`・`/rss.xml`・`/sitemap.xml` の GET のみ。`/api/**` と `/mypage` は対象外のまま。
- ログイン判定は `locals.user`（`getSessionUser` で解決済み）が null であることに加え、リクエストの Cookie に `sb-` プレフィックスの Supabase Auth Cookie が1つも無いことを二重にチェックする。`locals.user` が null でも `sb-` Cookie が残っていれば `private, no-store` にし、共有キャッシュに他人のセッション依存HTMLが乗る事故を防ぐ。
- 許可時は `Cache-Control: public, max-age=60, s-maxage=300, stale-while-revalidate=600` + `Vary`（既存値を保持したまま `Cookie` を追記）。対象外・ログイン時は `Cache-Control: private, no-store` を明示する（無指定より安全）。
- 判定ロジック（パス判定・Cookie判定・ヘッダ決定・Vary合成）は `src/lib/cache-policy.ts` に `astro:middleware` 非依存の純粋関数として切り出し、`tests/cache-policy.test.ts`（11件）で単体検証した。
- **既知の制約**: `astro dev` では `getSessionUser`（`src/lib/user-session.ts`）が `import.meta.env.DEV` 判定で常にダミーの `DEV_SESSION_USER` を返す仕様のため、ローカルの `curl` 検証では「未ログイン→`public`系ヘッダ」の分岐を再現できない（`locals.user` が常に非nullになるため常に `private, no-store` になる）。これは今回のタスクで変更すべきでない既存の開発用ショートカットと判断し、そのロジックはユニットテストで担保するに留めた。本番ビルド（`import.meta.env.DEV` が false）では通常どおり `getSessionUser` が実際のセッションを解決する。

### 検証
- `curl -sI http://localhost:4321/` / `/items` / `/tags/python` / `/rss.xml` → `cache-control: private, no-store`（上記の理由により dev では常にこの値になる。想定どおり）。
- `curl -sI -H "Cookie: sb-test-auth-token=x" http://localhost:4321/` → `private, no-store`。
- `curl -sI http://localhost:4321/mypage` → `private, no-store`。
- `curl -sI http://localhost:4321/api/items` → `Cache-Control` ヘッダなし（`/api/**` 未変更を確認）。
- `npm test`（26件、うち `cache-policy.test.ts` が新規11件）・`npm run build` いずれも成功。

### 残課題
- `feat/anonymous-cache` ブランチはマージしていない（指示によりブランチのまま終了）。本番デプロイ後、未ログインでの実アクセスで `public` 系ヘッダが出ることを別途確認するとよい。

## 追記: canonical/noindex 方針の実装と sitemap の lastmod・タグ一覧追加（同日後続作業、`feat/seo-canonical`）

`main` から `feat/seo-canonical` ブランチを切り、SEOのURL正規化を実装した（**マージはせずブランチのまま終了**）。

### 実装内容
- `src/layouts/Layout.astro`: Props に `canonicalPath?: string`（省略時は現在の pathname）と `noindex?: boolean`（既定 false）を追加し、`<link rel="canonical">` を新規出力、`noindex` 時のみ `<meta name="robots" content="noindex, follow">` を出力するようにした。`og:url` もこの `canonicalUrl` を使うため、ページ側が `canonicalPath` を渡した場合は og:url も揃って正規化される。
- `src/pages/items/index.astro`: `q`/`sourceId`/`sort`/`period`/`page` のいずれか、または `tag` が付いていれば `noindex=true`。ただし `tag` を1つだけ指定し他のクエリが無い場合は `canonicalPath` を `/tags/{tag}` に寄せる（実質タグページと同じ内容のため）。それ以外は `canonicalPath="/items"`。
- `src/pages/tags/[name].astro`: `canonicalPath` を常に `/tags/{name}`。`page>1` は薄い重複ページとして `noindex=true`。
- `src/pages/index.astro`（トップ）: `canonicalPath="/"`。`tag`/`sort`/`page` のいずれかが付いていれば `noindex=true`。
- `/about`・`/tags`（一覧）は `canonicalPath` 未指定のままとし、Layout の既定動作（pathname のみ、クエリなし）に任せた。
- `src/pages/sitemap.xml.ts`: `fetchCatalogItems({ limit: 1 })` で得た最新記事の `published_at ?? created_at` を `/` と `/items` およびタグ一覧ページ共通の `lastmod` として付与（タグ別の最新記事日時までは追わず、全タグ共通の値で割り切る旨をコメントに明記）。sitemap に `/tags`（タグ一覧）と `/about` を追加した。

### 検証
- `npm test`（26件）・`npm run build` いずれも成功。
- 起動中の dev サーバー（本番データ）に対して curl で確認:
  - `/items?q=x&page=2` → `<link rel="canonical" href=".../items">` + `<meta name="robots" content="noindex, follow">`。
  - `/items?tag=python` → `canonical` が `.../tags/python` に正規化される。
  - `/` → `canonical` は `.../` のみで robots メタなし。
  - `/?tag=python` → `canonical` は `.../` のまま、`noindex` あり（トップのタグ絞り込みは canonical をタグページへ寄せる対象外、noindex のみで対応）。
  - `/tags/python` → canonical のみ。`/tags/python?page=2` → 同じ canonical + noindex。
  - `/sitemap.xml` → 各 URL に `lastmod` が付き、`/tags` と `/about` が列挙されている。
- 変更ファイルは `src/layouts/Layout.astro`・`src/pages/index.astro`・`src/pages/items/index.astro`・`src/pages/sitemap.xml.ts`・`src/pages/tags/[name].astro` の5点のみ。指示どおり未追跡ファイル（`migrations/014_add_import_runs.sql`・`scripts/db/backfill-tag-explanations.mjs`・`scripts/db/detect-duplicate-items.mjs`）には触れていない。

### 残課題
- `feat/seo-canonical` ブランチは未マージ。レビュー後に PR 作成・マージが必要。
- タグページの `noindex` は `page>1` のみで判定しており、1ページ目でも記事0件のタグ（空のタグページ）は対象外のまま（今回のスコープ外）。

## 追記: 運用・信頼性の改善一式（同日後続作業、`feat/ops-reliability`）

`main` から `feat/ops-reliability` ブランチを切り、収集ジョブの実行履歴記録・CSRF対策・書き込みレートリミット・監視方針の文書化を実装した（**本番Supabase/Cloudflareへは一切触れず、マージもせずブランチのまま終了**）。

### 1. 収集ジョブの実行履歴記録（import_runs）
- 未追跡だった `migrations/014_add_import_runs.sql`（本番適用済み、既存の列で今回の要件を満たすため015の追加は不要と判断）をこのブランチのコミットに含めた。
- `src/lib/import-runs.ts` を新設。`recordImportRun`（1行insert、失敗はtry/catch+console.errorに留めジョブを壊さない）、`fetchImportRuns`（直近50件、finished_at降順）、`runAndRecord`（成功/失敗を自動判定して記録しつつ結果を返す・エラーは再throwする共通ラッパー）を実装した。
- `src/worker.ts` の4つのcronハンドラ（Qiita/Zenn/note/blog）を `runAndRecord('<provider>', 'cron', ...)` でラップした。既存の `sendOperationalAlert` によるWebhook通知は維持（`runAndRecord`が再throwしたエラーを既存のcatch節で受ける形にしたため、既存ロジックへの変更は最小限）。
- `src/pages/api/import/{qiita,zenn,note,blog}.ts` のPOSTも同様に `runAndRecord('<provider>', 'api', ...)` でラップした。
- `src/pages/api/import/runs.ts` を新設（`GET`で直近50件を返す）。実行履歴自体が管理者向け情報のため、`src/middleware.ts` の `requiresAdminAuth` に `/api/import/runs` を明示的に追加し、GETであっても常時Basic認証必須にした（`/api/audit` と同じ扱い）。

### 2. 状態変更APIのCSRF対策（Origin検証）
- `src/lib/csrf.ts`（`isSameOrigin`）を新設。Originヘッダーが無ければ許可（curl・サーバー間連携）、あればリクエスト先オリジンと比較し不一致なら拒否する純粋関数。`tests/csrf.test.ts`（7ケース）で検証。
- `src/middleware.ts` の `/api/bookmarks`（Cookie認証で動く唯一の状態変更エンドポイント）向けの分岐に、書き込みメソッド（POST/DELETE、GETは対象外）のみOrigin検証を追加した。認証チェックより先に行うことで、Origin不一致は未ログインでも403になる。
- Supabase Auth CookieのSameSite設定を確認した。`node_modules/@supabase/ssr/dist/main/utils/constants.js` の `DEFAULT_COOKIE_OPTIONS` は `sameSite: "lax"`、`src/lib/user-session.ts` の `createServerClient` 呼び出しはcookieOptionsを上書きしていないため、本番も既定のLaxのまま。Origin検証と合わせて多層防御になっている（コード変更は不要と判断、変更なし）。

### 3. 書き込みAPIの簡易レートリミット
- `src/lib/rate-limit.ts`（`createFixedWindowRateLimiter`）を新設。固定ウィンドウ（既定60秒/30回）でキー（ユーザーIDまたはIP）ごとにMapでカウントする純粋な実装。「Cloudflareダッシュボードのレートリミットルールが本命で、これは多層防御の1層」である旨をコード内コメントに明記した。`tests/rate-limit.test.ts`（5ケース）で検証。
- `src/middleware.ts` で、`/api/bookmarks` の書き込みメソッドに対し、Origin検証・ユーザー認証チェックの後、`context.locals.user.id` をキーに `bookmarksWriteRateLimiter.check()` を呼び、超過時 429 + `Retry-After` ヘッダを返すようにした。

### 4. 監視系の方針文書化
- `docs/reference/operations.md` に「監視・アラート」節を新設し、(a) 収集失敗の `ALERT_WEBHOOK_URL` 通知（実装済み・シークレット未設定）、(b) `import_runs` + `GET /api/import/runs` による実行履歴、(c) Cloudflare Workers Logsを一次としSentry等は保留、(d) UptimeRobot等の外形監視は未設定、(e) Cloudflare Web Analyticsをアクセス解析の候補として保留、の5点を記載した。「障害対応の初動」節にも `GET /api/import/runs` での失敗確認手順を追記した。

### 検証
- Docker使い捨て `postgres:16`（ポート55434、CIと同じ `auth.users` スタブを事前投入）に対し `DATABASE_URL` を切り替えて `npm run migrate` を実行し、001〜014の全マイグレーションが空DBから順に適用できることを確認した。続けて `import_runs` に成功行・失敗行（provider/trigger/status/fetched等）をinsertし、`finished_at DESC` で正しく取得できること（`idx_import_runs_provider` の並びと一致）を確認後、コンテナごと削除した。
- `npm test`（38件、うち `csrf.test.ts` 7件・`rate-limit.test.ts` 5件が新規）と `npm run build` がいずれも成功することを確認した。
- 起動中のdevサーバー（本番Supabaseを参照、`.env`/`.dev.vars`の`SUPABASE_URL`で確認済み）に対し、書き込みを伴わない範囲でのみ確認した:
  - `GET /api/import/runs`（Basic認証なし）→ `401 Unauthorized`。
  - `GET /api/import/{qiita,zenn,note,blog}`（既存の読み取り専用メタデータ、変更なし）→ いずれも200のまま。
  - `GET /api/audit`（Basic認証なし）→ 既存どおり401（回帰なし）。
  - `POST /api/bookmarks` + `Origin: https://evil.example` + 空body → `403 {"error":"Origin not allowed"}`（DBへ到達する前に拒否されることを確認、本番への書き込みは発生していない）。
  - `POST /api/bookmarks`（Origin無し／一致するOrigin、いずれも空bodyでitemId欠如による`400`止まり）→ CSRFチェックは正しく通過し、既存のバリデーションで弾かれることを確認（本番への書き込みは発生していない）。
  - `GET /api/bookmarks`（読み取り、Origin不一致含む）→ 200のまま（CSRF検証は書き込みメソッドのみが対象であることを確認）。
  - 上記と合わせて同一エンドポイントへ空bodyのPOSTを合計35回連続送信し、30回目まで`400`（バリデーションエラー、DB未到達）、31回目以降`429`になることを確認し、固定ウィンドウレートリミッタが実際のミドルウェアチェーン上で機能していることを確認した（本番への書き込みは一貫して発生していない）。

### 変更ファイル
- 新規: `src/lib/import-runs.ts`・`src/pages/api/import/runs.ts`・`src/lib/csrf.ts`・`src/lib/rate-limit.ts`・`tests/csrf.test.ts`・`tests/rate-limit.test.ts`
- 変更: `src/worker.ts`・`src/pages/api/import/{qiita,zenn,note,blog}.ts`・`src/middleware.ts`・`docs/reference/operations.md`
- コミットに含めた未追跡ファイル: `migrations/014_add_import_runs.sql`（このタスクの一部として指示されたもの。スキーマ変更はせず本番適用済みの内容のまま）
- 指示により触れていない未追跡ファイル: `scripts/db/backfill-tag-explanations.mjs`・`scripts/db/detect-duplicate-items.mjs`（別タスク）

### 残課題
- `feat/ops-reliability` ブランチは未マージ（指示によりブランチのまま終了）。レビュー後にPR作成・マージが必要。
- `ALERT_WEBHOOK_URL`・UptimeRobot等の外形監視・Cloudflare Web Analyticsは、いずれも実際の値/アカウント設定が必要なため未着手のまま（方針のみ文書化）。

## 参照
- [development-roadmap.md](../development-roadmap.md)
- [2026-07-05.md](2026-07-05.md)
