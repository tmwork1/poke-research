# 進捗メモ 2026-07-07

## 今日の作業

ユーザーから「はてなブックマークからも収集したい」と依頼された。過去（[2026-07-04](2026-07-04.md)・[2026-07-05](2026-07-05.md)）に一度検討し、「はてなブックマークの検索RSSはブックマークされた全ウェブ横断の検索であり非公式寄りの利用になる」という理由で見送り、代わりにBrave Search API経由の個人ブログ収集（`blog.ts`）を採用した経緯があったため、この経緯をユーザーに提示して確認した。ユーザーは「キーワード横断検索（前回見送り案の再採用）」を選び、ToSの懸念は「経緯を承知の上でそれでも進める」と回答した。

### 収集精度の実地検証（実装前の事前調査）

計画確定のため、実装前に `b.hatena.ne.jp/search/text`・`/search/tag` の検索RSSを複数のキーワード（`ポケモン`単独、`ポケモン API`、`ポケモン+API`、`"ポケモン" "API"`、`ポケモン 実装`、`pokeapi`、`ダメージ計算 実装`）で実際にcurlし、生の結果を目視した。

- nonsenseクエリ（`asdkfjhaslkdfjhqwerty12345`）では0件になるため、検索自体は機能している。
- しかし**複数語を組み合わせても実質AND検索にはならない**。「ポケモン API」で検索しても、Anthropic/Claude Fable 5のニュース、AWSブログ、TypeScriptの技術記事など、ポケモンに一切言及していない話題性の高い最近の記事が上位に大量に混入した。クォート（`"ポケモン" "API"`）やプラス結合（`ポケモン+API`）でも同じ結果だった。
- 単独の「ポケモン」検索・タグ検索（`/search/tag`）でも、上位はポケモングッズ・アニメ・カードゲーム相場・育児エピソードなどで占められ、技術記事はほぼ皆無だった。
- 比較的まれな英単語「pokeapi」単独でも、Claude Code README記事・TypeScript Result型記事・Cursor iOSアプリ記事など無関係な最近の技術記事が混入した。
- `&ctgy=it`のようなカテゴリ絞り込みパラメータも効果が無かった（同じ結果セットが返る）。

これは `note.ts` で過去に確認した「母集団の性質上の問題」（[2026-07-05](2026-07-05.md)）と同種だが、対象が全ウェブのためnoteより悪化している。

### 判断: note.tsの前例に倣い実装・運用する

本プロジェクトには、この種の低精度母集団を許容してきた前例がある。note.ts はキーワード検索の生結果が「ほぼ0%が技術記事」と判明した後も、既存のAIレビュー（`article-ai.ts`、主題判定→技術判定の2段階）を安全網として収集を継続する方針を取り、クエリ側の対応は見送っている。この前例に倣い、**収集自体は実装・稼働させ、AIレビューに精度確保を委ね、原始候補数だけ保守的に絞ってOpenAIコストを抑える**方針を採用した。

ユーザーは確認直後に就寝し、「実装→レビュー→最適化→PR→マージまで自律的に」と指示したため、CLAUDE.mdの「mainへのpush・PRマージは実行前にユーザー確認」ルールをこのタスク限定で解除されたものとして最後まで自律実行した。

## 実装内容

`src/lib/importers/blog.ts`（Brave Search経由の個人ブログ収集）をテンプレートとして再利用し、差し替えたのは「候補発見（discovery）」部分のみ。本文抽出・AIレビュー・DB upsert・タグ同期のロジックはそのまま流用した。

- **新規 `src/lib/importers/hatena-feed.ts`**: はてなブックマーク検索RSS（RSS 1.0 RDF形式）の軽量パーサー。依存追加はせず、`<item rdf:about="URL">...</item>` ブロックを正規表現で抽出する純粋関数（`parseHatenaSearchRss`）と数値文字参照デコード（`decodeXmlEntities`）を実装。`cloudflare:workers`非依存のため`tests/hatena-feed.test.ts`で直接ユニットテストできる。
- **新規 `src/lib/importers/hatena.ts`**: `syncHatenaCollection`/`resolveHatenaSyncOptions`。discoveryは`POKEMON_KEYWORDS`（`keywords.ts`、変更なし）の各キーワードで検索RSSを1リクエストずつ取得し、`b.hatena.ne.jp/robots.txt`の`Crawl-delay: 5`に従いリクエスト間に5秒待機する。本文抽出・upsert・タグ同期は`blog.ts`から`extractPage`/`fetchCandidatePage`/`hashBody`/`resolveBlogSource`をexport追加して再利用（`blog.ts`側の変更は`export`キーワード追加とuserAgent引数化のみで、既存の挙動・呼び出しは変更していない）。発見したURLには既存の`isExcludedBlogDomain`（qiita/zenn/note/github等）をそのまま適用し、他インポーターとの重複収集を避ける。新規ソースのため候補数はキーワードあたり既定15件（`MAX_CANDIDATES_PER_KEYWORD`、`HATENA_MAX_CANDIDATES`で上書き可）に絞り、6キーワード×15件=最大90件/日の本文取得・AIレビューを上限とした。`sources.type`は`'blog'`のまま（はてなブログ等の既知プラットフォームはBrave経由発見分と同じsource行を共有）、provenanceのみ`{ source: 'hatena-bookmark-importer', keyword, fetched_at, hatena_bookmarked_at }`で区別する。ジョブ識別（`import_runs.provider`）には自由文字列`'hatena'`を使用（マイグレーション不要）。
- **`src/worker.ts`/`wrangler.jsonc`**: `HATENA_CRON = '30 23 * * *'`（JST 8:30）を追加し、他ジョブと同様のcron分岐・失敗時Webhook通知を実装。
- **新規 `src/pages/api/import/hatena.ts`**: 他インポーターと同型のAPIルート（GET: 既定値確認、POST: `runAndRecord('hatena', 'api', ...)`）。
- **新規 `scripts/collect/collect-hatena.mjs`**: 薄いPOSTラッパー（`npm run collect:hatena`）。
- **`scripts/collect/backfill.mjs`**: `hatena`ターゲットを追加したが、精度検証中のため既定の`BACKFILL_TARGETS`には含めていない（明示指定時のみ対象）。
- **新規 `scripts/eval/eval-collection-hatena.mjs`**: 収集クエリ精度の評価スクリプト（`npm run eval:collection:hatena`）。DB・APIキー不要で、AIレビュー前の生の検索RSS結果を出力する。`eval:all`のデフォルト実行にも組み込んだ（Brave経由のブログ評価と異なりAPIキー課金が無いため）。
- **テスト**: `tests/hatena-feed.test.ts`（新規7ケース）でRSSパース・エンティティデコードを検証。
- **ドキュメント**: `docs/reference/scripts.md`・`README.md`（はてなブックマーク収集ジョブ節を新設）・`docs/development-roadmap.md`（M3拡張リストに追加）・`package.json`（`collect:hatena`/`eval:collection:hatena`エイリアス）を更新。

## 検証

- `npm run build` 成功。
- `npm test`（76件、うち`hatena-feed.test.ts`7件が新規）全パス。
- `npm run eval:collection:hatena` を実際に実行し、6キーワード全てで実測データを取得。「pokeapi」キーワードで除外ドメイン後に残った候補のうち、明確に無関係なもの（TypeScript Result型記事、uvの新機能記事、Cursor iOSアプリ記事等）が大半を占め、ポケモン関連技術記事は`motemen.hatenablog.com`の「全ポケモンの名寄せデータ POKEMON_ALL.jsonを作った」1件程度だった。他キーワード（ポケモン、ポケモン API、ダメージ計算 実装等）はほぼ全て無関係の結果だった。
- ローカル`astro dev`（ローカルSupabaseスタック、本番とは別DB）に対し`POST /api/import/hatena`を`keyword=pokeapi, maxCandidatesPerKeyword=3`で実行し、実際にend-to-endの動作を確認した。
  - fetched=3, inserted=2, skipped=1。
  - 除外ドメイン判定が正しく機能し、生結果に含まれていた`zenn.dev`/`qiita.com`の記事は候補数にカウントされずスキップされた。
  - AIレビューが正しく機能: gihyo.jpの「uvの新機能」記事は「主題がポケモン関連ではない」として正しくskipped。tech.naturalmindo.comの「Claude CodeでREADME.mdのアーキテクチャを適用させたら素敵なアプリができた」は、タイトルだけでは分からないが本文がPokéAPIを使ったポケモン図鑑アプリの実装解説だったため正しくinserted（true positive、要約・タグとも妥当）。motemen.hatenablog.comの記事も正しくinserted、`sources`テーブルは`resolveBlogSource`により`はてなブログ`（`origin_url: https://hatenablog.com/`）として既存のBrave経由ブログ収集と同じバケットに解決されることを確認した。
  - 上記により、生の収集精度は低いものの、AIレビューを安全網として使う設計判断（note.ts前例踏襲）が実際に機能することを確認できた。

## 残課題・注意点

- はてなブックマーク検索の生の精度は低い（キーワードにより体感0〜10%程度が真に技術関連）。cronで毎日90件程度の本文取得・AIレビューが走るため、他ソースよりOpenAIコストが高くなる。実運用後、コストと採用率を見てキーワード構成や`HATENA_MAX_CANDIDATES`の調整、あるいは収集自体の停止を再検討する余地がある。
- マイグレーション変更は無いため本番Supabaseへの追加適用は不要。

## 記事カード・検索ページのUI改善（要望5件）

ユーザーから箇条書きで5件のUI改善要望を受け、`feat/item-card-ux-and-source-counts`ブランチで実装した。

1. **タイトルの下線削除**（要望は「アンダースコア」だったが、確認したところ実際は下線＝`text-decoration`の意）: `ItemCard.astro`のタイトルは`<a>`と`<p>`の出し分けだったが、2の対応でタイトル自体を常に`<p>`（非リンク）にしたことで解消した。
2. **タグ以外クリックで元記事へ**: カード全体を「stretched link」パターン化。`.card`に`position:relative`を付け、カード末尾に透明な`<a class="card-link">`（`position:absolute; inset:0; z-index:1`）を追加。タグ・共有ボタン・ブックマークボタンは`z-index:2`で手前に出し、独自のクリックを維持。既読トラッキングも旧`a.item-title`から`.card-link`のクリックへ付け替えた。
3. **タグ: クリックで検索、ホバー/長押しで解説**: タグを`<span role="button">`から実リンク（`href="/tags/{tagName}"`）に変更。解説ポップオーバーは`mouseenter`/`focus`（250ms遅延）と`touchstart`長押し（500ms）でのみ表示するようにし、長押し直後のクリックだけ`preventDefault`して意図しない遷移を防いだ。
4. **ソースボタンに件数表示・多い順ソート**: `migrations/019_add_source_item_counts.sql`でRPC `source_item_counts()`を追加（`link_status != 'broken' AND ai_accepted`の可視条件はtop_tagsやqueryCatalogItemsと揃えた）。`catalog.ts`の`fetchCatalogSources()`をこのRPCと結合し、件数降順でソートして返すよう変更（戻り値型を`Source[]`から`SourceUsage[]`へ）。
5. **圧縮表示ボタンの配置**: `CompactToggle`をeyebrow行から、検索結果グリッド直前の上側`Pagination`と同じ行（`.top-controls`、`position:relative`＋`.compact-toggle-slot`を`position:absolute; right:0`で右端固定）に移動した。

### 検証
- `npm run build`成功。
- ローカルSupabaseスタック（本番とは別DB、`.dev.vars`経由）にも`migrations/019`を適用し、`astro dev`で`/items`・`/`・`/mypage`・`/tags/codex`のレンダリングを確認。ソースチップの件数・降順（Zenn 10 > Qiita 3 > はてなブログ 1 = その他 1 > note 0）、タグの`<a href="/tags/...">`化、`card-link`のマークアップを実際のHTML出力で確認した。
- **本番Supabaseにも`migrations/019`を適用済み**（`npm run release`、ユーザー確認済み）。RPCの反映にPostgREFTのスキーマキャッシュ再読み込みが必要で、`NOTIFY pgrst, 'reload schema'`を送って即時反映させた。

### 残課題
- ホバー/長押しでのタグ解説表示、カード全体クリックの実際のブラウザ操作感（特にモバイルの長押し）は目視確認できていない。マージ前に実機/ブラウザでの確認を推奨。
- PR作成・マージは未実施（本会話時点）。

## タグ最適化（`chore/tag-optimization`ブランチ）

ユーザーから「AIによるタグの最適化（大/小文字の正式名称化、短縮化、不適切タグ削除）」を依頼された。本番タグ162件を全件取得し、使用件数・記事タイトルを確認したうえでClaude Codeが直接内容を精査し、以下を実施した。

### Phase 1: 本番タグ台帳の一括整理（実施済み）
- リネーム: `ポケモン図鑑`(130件)→`図鑑`、`ポケモン対戦分析`(106件)→`対戦分析`、`openai-codex`(6件)→`codex`、`raspberrypipico`(1件)→`raspberry pi`（ユーザー指定で`pico`は落とした）。
- 統合: `ポケモン画像認識`→`画像認識`、`rom-hacking`→`rom改造`。
- 削除: `テスト`（1件、汎用語で絞り込みに使えない）、`課題管理`（1件、記事内容と結びつかない一般語）。
- 明示的に据え置き: `ポケモンカード`（「カード」だけだと意味が曖昧になるため）、`hyper`/`hyper-pokemon`（実在のnpmパッケージ名で重複ではない）、`初心者`/`教育`（一般語だが連載シリーズで実用性がある）。

**実装上の注意**: 既存の`merge-tag.mjs`は`--dry-run`未対応と気づかずdry-run実行したつもりが2件（画像認識・rom改造の統合）は実際に即時反映された。結果は承認済みプラン通りだったため実害は無かったが、ユーザーには実行後すぐに申告した。

### 新規スクリプト
- `scripts/db/rename-tag.mjs <from> <to> [--dry-run]`: 統合ではない単純リネーム。to が既存タグの場合は`merge-tag.mjs`を使うよう案内する。
- `scripts/db/delete-tag.mjs <tag> [--dry-run]`: 不適切・低情報価値タグの削除。
- `scripts/db/optimize-tags.mjs`: タグ台帳全体を1回のOpenAI呼び出しに渡し、リネーム/統合/削除の提案と適用コマンド例を出力する読み取り専用スクリプト（Phase 2、再利用可能な定期メンテ用）。今回は実装のみでユーザーの指示により未実行（OpenAI課金が発生するため）。

### コードのみの変更（本番DB書き込み無し）
- `src/lib/tag-display.ts`の`KNOWN_LABELS`に、自動大文字化ロジックでは正しく表示できない略語・固有表記（`HTTP`/`RAG`/`DDD`/`ChatGPT`/`LangChain`/`macOS`/`scikit-learn`/`gorilla/mux`など約25件）を追加。
- `tests/tag-display.test.ts`に回帰テストを追加。

### 検証
- `npm test`（77件）全パス。
- `npm run build`成功。
- Phase 1の6操作は実行前に全て`--dry-run`（画像認識・rom改造統合の2件は前述の理由で実質本番反映）で内容確認し、実行後に`top_tags`で反映結果を確認した。

### 残課題
- `scripts/db/optimize-tags.mjs`は未実行（動作未検証）。次回実行時にOpenAI課金の事前確認とレスポンス形式（JSON配列 or `{tags:[...]}`）の実際の挙動を確認すること。
- PR作成・マージは未実施（本会話時点）。マイグレーション変更は無いため本番Supabaseへの追加適用は不要。

## DB重複候補の週次レビューをCron化

ユーザーから「DBのreview/cleanupをサーバー側で定期実行できないか」と依頼された。既存の`scripts/db/detect-duplicate-items.mjs`/`detect-duplicate-sources.mjs`（DB非書き換えの重複候補検出）と`optimize-tags.mjs`（OpenAI呼び出しを伴うタグ最適化提案）を精査し、以下の方針を提案してユーザーに確認した。

- タグ最適化提案はOpenAI課金が実行の都度発生するため自動化対象から除外（従来通り手動実行）。
- 重複候補検出（items/sources）はDBを書き換えず課金も発生しないため、既存の日次収集Cron群と同じ仕組み（`src/worker.ts`の`scheduled`ハンドラ）に乗せて自動化してよい。
- 実行タイミングは毎週月曜、日次収集ジョブ群（Qiita 21:00 UTC〜はてな 23:30 UTC）の直前である20:30 UTCに配置。

### 実装内容
- **新規 `src/lib/maintenance-review.ts`**: `detect-duplicate-items.mjs`/`detect-duplicate-sources.mjs`のロジックをTypeScriptへ移植（`normalizeUrl`/`stripSymbols`/`levenshtein`/`isSimilarTitle`/`isSimilarName`）。`runWeeklyReview()`で両方を実行し、`formatWeeklyReviewMessage()`でWebhook向けの要約テキスト（候補は各セクション先頭5件まで）を生成する。DBは一切書き換えない。
- **`src/lib/notify.ts`**: Webhook投稿処理を`postToWebhook`に共通化し、エラー通知用の`sendOperationalAlert`（⚠️）とは別に、定期レポート用の`sendMaintenanceReport`（📋）を追加。
- **`src/worker.ts`/`wrangler.jsonc`**: `WEEKLY_REVIEW_CRON = '30 20 * * 1'`を追加。他の収集ジョブと同様、失敗時は`sendOperationalAlert`で通知する。

### 未対応・注意点
- `ALERT_WEBHOOK_URL`シークレットは本番Cloudflareに未設定（[2026-07-06](2026-07-06.md)で既知）。設定するまでは週次レビューの結果もこれまでの失敗通知と同様に実際には届かない。運用を始めるなら`wrangler secret put ALERT_WEBHOOK_URL`が必要（本番操作のためユーザー確認の上で実行）。
- 検出ロジックはitems/sources全件のO(n²)比較のため、件数が大きく増えた場合はWorkerのCPU時間に注意（現状270件程度では問題なし）。

### 検証
- `npm run build`成功。

## 参照
- [development-roadmap.md](../development-roadmap.md)
- [2026-07-05.md](2026-07-05.md)（過去の見送り経緯）
- [2026-07-06.md](2026-07-06.md)（`ALERT_WEBHOOK_URL`未設定の経緯）
